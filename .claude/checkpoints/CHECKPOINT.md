# 체크포인트: MCP 자동 바닥 ROI 생성 — 이미지에서 4점 검출 (goal/loop B 모드)
_갱신: 2026-07-29 11:00 · 트리거: 압축(마스터 지시 — 컨텍스트 70%↑ 시 메모 후 클리어)_

## 목표 (성공기준)
2개 카메라 전 프리셋(23면)에서 **주차면 개별 IoU ≥ 0.98**.
**조건: 이미지에서 사각형 4점을 직접 검출**(수동 ROI 는 채점에만 — hold-out). 실카에서도 성립해야 함.

## 계획·진행 (루프 10회차)
- [x] 1회차 — 도색선 검출 + 호모그래피. 평균 0.09116, 0/18
- [x] 2회차 — 도색지지 점수. 0.15560. **소실점 붕괴 원인 규명**(스텁 24px → ±2.4°)
- [x] 3회차 — **f 를 제원에서 주입**(경로 교체). 0.57942. `2:1` 0.033→0.931
- [x] 4회차 — 높이 자가보정(4.951/4.960m). 0.58383
- [x] 5회차 — `aimCenterWeight` 도입. 0.72496. `1:2` 0 탈출
- [x] 6회차 — LOPO 로 `aimCenterWeight` **과적합 의심** 판정. 수치향상 0(가장 값진 라운드)
- [x] 7회차 — **잔차 분해**: 13면 결손 전체가 **근변선 오프셋** 단일 성분
- [x] 8회차 — 2-DOF 재적합. **최초 ≥0.98 면 3개**. 0.74229
- [x] 9회차 — 24관측 LOPO → `aimCenterWeight` **「검증됨」**. `2:2` 「임계」 재분류
- [x] 10회차 — **수렴반복 가설 반증.** 재적합은 덜 돈 게 아니라 **이미 수렴한 고정점이 편향**(2·5회차 바이트 동일). 감쇠 0.5 도 IoU 하락. 후보원인 **7개 제거**. 수치 불변(0.74229, 3/21)
- [x] 11회차 — **규약 가설 반증**(`r`=−0.08~−0.11, ±1 이어야 모서리 → **수동·자동 둘 다 중심선**). 소거 **9건**. 대신 **새 단서**: 도색 중심선이 이미지에서 **직선이 아니다**(국소 중심이 적합직선 대비 1.1~1.3px 드리프트, 수동은 그 곡선을 **따라간다**). `tM−tC` 가 두 프리셋에서 +0.501/+0.498px 로 일치
- [x] 12회차 — **★ `2:2` 해결: 0 → 0.94410**(다시점 합의). 평균 0.56883→**0.75668**. 잘 되는 프리셋은 6시점 만장일치 → 기저가 대표로 남아 **비트 동일**(구조적 무회귀). **리더 인공물 가설 반증**(폭 비례 창도 드리프트 불변 1.17 vs 1.11px). 소거 **10건**
- [x] **14회차 완료 — 이 라운드의 결론은 수치가 아니라 타당성이다**
  - ★ **프레임 종속**: 같은 PTZ·같은 코드인데 잡힌 프레임에 따라 ≥0.95 통과면수가 **±6면**(1:1 `65182cdf`→0/7 vs `6006a034`→7/7). 각각 재현 100% = 잡음 아닌 결정론. **프레임 해시 없는 IoU 는 해석 불가**
  - ★ **7~13회차 회차간 비교(0.72496→0.74229→0.75668)는 무효** — 알고리즘 개선인지 프레임 운인지 구분 불가
  - ★ 서비스 붕괴 원인 = **12:46 `selectedCameraId: "real-camera-2"` 전환**. 실카 프레임을 시뮬 정본으로 채점 중이었다. 서비스 실행 3건 폐기. 13021 은 무죄(로그 00:48 이후 0바이트)
  - **tilt 미보정은 실제 결함** — `roiAuto.ts:detectOne` 이 디더 프레임에 기저 tilt 사용. 고정프레임 통제비교로 `2:2` **0→0.94182**, ≥0.95 **11→13면**
  - P2 좌표융합 **3기준 전부 실패**(13→10면) → 서비스 미배선, `src/tools/roiAutoFuse.ts` 에 진단용 보존
  - **골든 프레임 세트 확립**: `test/fixtures/roiAutoGolden/` 30장 + manifest (10.0MB). **앞으로 알고리즘 비교는 이 위에서만**
  - P4 완료 — `bayGrid.ts` 봉인 추가(green, 오염 없음) + 신규 모듈 자동 검출 **메타 테스트**
- [x] **15회차 완료 — 방향 전환(시뮬 점수 트랙 종료 → 실카 이행 준비물 공장). 측정 라운드**
  - ★★ **재적합 교락 발견**(설계자): `bayGrid.ts:508` 조기반환이 ③ 근변선 재적합(523~556) **앞**에 있어 `calibrateHeight:false` 가 **재적합까지 함께 끈다**. → **14회차 `CALIB=0` 수치는 "보정+재적합을 껐더니"로 정정**. 통제군 **A0**(보정 ON + `refitFrontLine:false`) 추가 → 재적합 단독 기여 **5면**(v1 A 13면 → A0 8면)
  - ★ **QA 가 구현자 1차 결론을 반증**: "B 는 프레임 무관(G2=0.00000)"은 `toFixed(5)` 반올림 산물. 실측 격차 **3.3×10⁻⁷**. 프레임 주입 실험에서 **0.95579→0.25387** → B 도 프레임 의존. **살릴 문장 = 감도비**: 동일 섭동(MAD 0.055)에 A 0.11933 · A0 0.09192 · C 0.09119 vs B 0.00000033 → **약 3.6×10⁵**. 증폭 경로 직접 관측(자가보정 지상고 4.956→5.051, +1.92%)
  - ★ **P1 전제 미지지**: v2 30장 전부 `attempts=2`·`settled:false` **0건** → v1 도 이미 정착본. v1↔v2 차이는 "이동직후 vs 정착"이 아니라 **캡처 시각(13:38 vs 15:29)**. 동일 해시 **22/30**(2:1 은 **6/6 동일** → 지시서 기준① 2:1 격차는 **측정 불성립**)
  - **성공기준 판정**: ① 미측정(2:1 동일프레임) — 대체측정으로 **증폭기 가설 지지** / ② 만장일치 무회귀 ±0.002 **B·C 모두 미달**(B 2:1 **+0.07752**, 1:1 −0.01895) / ③ **B·C 모두 통과**(A 3면 → B 4면·C 5면). **②를 아무도 못 넘겨 16회차 배선 후보 없음 · 서비스 무변경**
  - **C 2:2 = 0 붕괴는 버그 아님**(QA 확정): 득표 동점 해소 문제 — C 가 고른 5.0327m 에서 군집이 파편화(2:2 동점) → `roiAutoFuse.ts:337` 최소인덱스 규칙이 틀린 행 latch 한 d0 을 뽑음. 2:2 는 4.950→5.033 에서 갈리는 **임계 프리셋**
  - **2:1 잔여 결손**: 증폭기 제거 후에도 B 6면이 0.9256~0.9375 에 **균일하게** 깔려 0.95 에 평균 **0.018** 부족(산포 아닌 일정 편향) — 16회차 표적 입력
  - 산출: `test/fixtures/roiAutoGolden_v2/`(30장+manifest, `settled`/`attempts`/`settleMs`, 11MB, **git 미추적**) · 신규 `src/tools/roiAutoGoldenV2.ts` · 확장 `src/tools/roiAutoFuse.ts`(v1/v2 × A/A0/B/C). **`src/ground/*`·서비스·정본·DB 무변경**
  - 회귀: `tsc --noEmit` exit 0 · `vitest` **281파일 3553 green**(QA 직접 재실행)
- [x] **16회차 — 뷰어 UI 배선**(도색선 자동검출 패널). 신규 `web/autoPaint.js` + `#roi-autopaint` 가산 레이어(시안). `roi.auto.apply` **미배선**(정본 보호)
- [x] **17회차 — 소스 불일치 수정 + 실카 제원**(3 이터레이션)
  - ★ **화면이 거짓말하고 있었다**: `roi.auto.*` 에 소스 파라미터가 없어 **시뮬 프레임을 실카 화면 위에 그리고 있었다**. `source` 파라미터 + `usedSource` 응답 추가로 해결
  - 제원 없는 소스는 **명시적 거부**(`INTRINSICS_MISSING`) — 시뮬 값으로 조용히 대체하지 않는다
  - **실카 화각 ×4.64 오류 발견·수정**: 마스터의 58.5°는 **광각단** 값. 현재 줌(zoompos 10677)의 실측 화각은 **13.77°** → f 1714 → **7952px**. `packages/lens-calib/src/presets.ts` 의 `cam-001` 실측표 13점을 상속(복제 0)
  - **기존 확립사실 2건 정정**: `real-camera-2` 는 캘리브 **있다**(cam-001 상속, `verdict:pass` 17/18) / tilt 부호·단위 **실측 확정**(`geometry.ts:3-6`, `tiltpos+ = 아래`, centidegree)
  - 실카 낮 프레임 **예약 캡처 5회 등록**(`ParkAgent_RealCamDaylight_{0633,0733,0833,1033,1233}`, PTZ 무이동 검증)
- [x] **18회차 — 개수 제거·다중 행·씬 정답** ★★ 측정 기반이 통째로 바뀐 라운드
  - ★ **`expectedBays` 가 정답지에서 왔다**(`roiAuto.ts:364`) → 15라운드 수치는 전부 "개수를 알려준" 값
  - ★ **씬 정답 확보**: Unity `preset.list`+`cam.list` 로 복원 → 정본을 **0.007px·IoU 0.99999+** 로 재현. **실제 가시 41면 vs 정본 23면(56%)**. `sceneTruth.ts` 는 채점 전용 정적 봉인
  - **F6(설치고 4.950m) 재확인** — 거리 스윕에서 4.95 가 정본을 0.007px 로 재현, 5.00 은 IoU 0.852. 리더의 "F6 의심" 판단은 **철회**
  - 마스터 지시 **58° 검산 통과**: f@zoom1 1731.89px → HFOV **58.0000°**(오차 4e-6도). 제원 경로 변경 0줄
  - 성적(씬 41면 분모): `best` 재현율 0.4146·정밀도 0.7083·매칭IoU 0.92915 / `rows` 재현율 0.5854·**정밀도 0.1832**
  - 무회귀 **원시 배정도 60/60**(15회차 로그가 `toFixed(4)`라 직전 코드를 복원해 동일 프로세스 대조)
- [x] **19회차 — 다중행 정밀도** ★ 목표 3개 전부 달성
  - 원인: **`rows` 에 진입 문턱이 없었다**(33행×3.97quad=131 산출). dedup 은 해법이 아니었다(214쌍 중 186쌍 겹침 0.000 = 서로 다른 엉뚱한 위치)
  - **`paint.score` 절대값엔 골짜기 없음**(프리셋별 최대가 1.7배 차이) → **`ratioBest`(best 대비 상대비)에 골짜기 존재**(0.916↔0.969). `rowMinScoreRatio=0.94`·`rowMinNearSupport=0.69`
  - **정밀도 0.1832 → 0.8571** (오검출 107개 중 103개 제거, **진짜 면 0개 손실** — 매칭 집합 완전 동일). 재현율 0.5854·매칭IoU 0.88860 **유지**
  - 구조 제약(평행·등간격)은 **측정 후 기각** — `1:2` 의 진짜 두 행이 **89.6° 직교**한다. 넣었으면 진짜 행을 잃었다
- [x] **20회차 완료 — 「현재 화면 그대로 검출」**(마스터 2026-07-30 지시). 목표②가 아니라 **실카 이행 경로**로 표적 교체
  - ★ **V1 통과**: 「검출」이 카메라를 안 움직인다(임의 뷰 PTZ 전후 원시 배정도 동일 · 리더 독립 재확인). 대조군 preset 모드는 움직인다
  - ★ **`f = f@zoom1 × zoom` 경로 신설** — 5프리셋 앵커 검산 `|Δf| ≤ 0.001px`(기준 0.5px). `f@zoom1` = 1731.8853px ↔ HFOV 58.00002°
  - ★ **§0-1 함정 근본 해결** — 마스터의 58°는 맞았고 코드가 틀렸다(`cameraIntrinsics.ts:70` 이 zoom 을 안 곱함). 「제원 3칸 비워라」 회피책 불필요
  - ★★ **이 라운드 최대 발견 = 격자 위상(along) 반칸 어긋남.** 같은 프레임·같은 PTZ 인데 `expectedBays` 4→8 이면 검출 격자가 반칸 밀린다(IoU 0.91~0.96 → 0.33~0.40, 전부 매칭문턱 0.5 **바로 아래**). **정답 위상 점수 1.568254 가 선택된 1.403061 보다 높은데 졌다** = 증거 결함 아닌 **선택 결함**
  - 원인 확정: `paint.score`(칸당 **평균**) vs `coverage`(칸 **개수**) **척도 불일치**. 반칸 위상은 온전한 1칸을 반쪽 2칸으로 쪼개 개수를 늘린다. **역전점 denom=7** 을 계산으로 짚고 스윕이 확인(bays 6→7 에서 재현율 0.6000→0.0000). 유일 생존 경로는 `bayGrid.ts:393-394`
  - **수정안은 배선하지 않았다**(G3 탈락): 골든 재현율 0.5854→0.4878 · 정밀도 0.8571→0.6250 · IoU 0.88860→0.90644 = 2/3 악화 → 되돌림. **검출 알고리즘 0줄 변경**
  - 7회차 "근변선 오프셋"은 **cross 방향**, 이건 **along 방향** = 별개 성분. 반증목록 20건에 위상 선택 항목 없음
  - ★ 임의 뷰 최고 성적: `pan 41.5/tilt 20.1/zoom 1.30`(프리셋 아님) 매칭IoU **0.96512 · 7/8 ≥0.95**(프레임 `883895192e57`) — 19회차 골든 0.88860 대비. **단 분모가 달라 직접 비교 금지**
  - **QA 「테스트 10건 삭제」 지적은 오탐**(개수는 정확, 귀속이 틀림): 원인은 마스터 별건 요청으로 **00:03 에 처리된 「지면 격자 패널」 UI 제거**(`docs/20260730_000316_지면격자패널_UI제거.md`, 그 문서가 main 287파일/**3651** 기록). 세션 시작 00:18 보다 **앞선다** → 20회차는 테스트 0건 삭제. 인계서의 3661 은 제거 **이전** 값
  - ★ **신규 규율**: 착수 시 인계서만 읽지 말고 **`docs/` 를 당일 날짜로 정렬해 훑어라**. 인계서(21:55) 이후 별건(00:03)을 놓쳐 기준선이 10건 어긋난 채 시작했다. 두 작업이 같은 파일(`web/index.html`·`app.css`)을 건드렸다
  - **미추적 파일에 `git diff` 를 쓰면 조용히 통과한다** — `bayGrid/bayGeometry/floorPaint/roiAutoScore` 는 전부 `??`. QA 가 ①원본 코드 직접 확인 ②mtime ③골든 재현 3중 증거로 대체
- [ ] **21회차 표적(리더 확정 순서)**: ① `rows` 진입 문턱을 `effectiveScore` 에서 **분리**(지금은 위상 실험이 `refScore`(`bayGrid.ts:632-640`) 경유로 골든을 오염시켜 A/B 자체가 불가) → ② **위상 불변 분모**(F18). ③ `phaseFitWeight` 상향은 **반증목록 5번 그대로 유효 — 재시도 금지**(리더 판정. 단 `phaseFitWeight`=0 이라는 사실은 기록)
- [ ] **이월(미착수)**: ①`bayGrid.ts:508` 조기반환(→"고정 지상고+재적합 ON") ②`roiAutoFuse.ts:72-73` 골든 판정 문자열 완전일치(`resolve()` 통일) ③`roiAutoFuse.ts:337` 대표시점 동점 규칙 ④봉인 테스트를 **전이 import 검사**로 강화(`bayGrid→autoRoiPlan→placeRoi` 부채)
- [x] ~~13회차~~(구 항목) — P0(40%) **JPEG 압축 가설**(`cam.capturePNG` 무손실 대조) / P1(40%) **합의를 서비스에 배선**(현재 도구레벨 측정일 뿐 `roi.auto.*` 미반영 → `2:2=0.944` 는 아직 산출물 아님) / P2(20%) 지면 평면성
- [x] **qa-tester·documenter 단계 실행됨(20회차)** — `_workspace/03c_qa_report_round20.md`(V4~V8, 독립 재실측 + 일부러 깨뜨리기 3/3) · `_workspace/04c_doc_impact_round20.md` · `docs/20260730_020139_MCP자동바닥ROI_20회차_현재화면그대로검출.md` · `docs/20260730_020510_20회차_인계서_다음세션용.md`. **CLAUDE.md 5대 규칙 중 15회차 이후 미실행이던 검증·문서화가 해소됐다**
- [ ] **21회차 이월(QA 발)**: ①`roiAutoRecall` 매칭 IoU 가 `toFixed(5)` 출력이라 5e-6 미만 변화 미검출 → `--raw` 덤프(「무회귀는 원시 배정도」 규율의 구멍) ②봉인 테스트가 `src/tools/**` 미검사 → `roiAutoCurrentViewOverlay.ts:77` 정본 읽기가 구조적으로 안 걸림

## 지금 상태
**평균 IoU 0.09116 → 0.74229, 개별 ≥0.98 = 3/21.** 목표 미달이나 원인이 전부 규명된 상태.
남은 최대 표적은 **13면 중 10면이 0.98 바로 아래** — 2-DOF 재적합이 회전을 **지나쳐 보정**(부호 반전)하는 오버슈트가 유일한 원인. 10회차 P1 이 이걸 친다.
회귀 **281파일 3552테스트 전량 green**, `tsc` exit 0, **정본·DB 무접촉**, `roi.auto.apply` 미실행.

## ▶ 다음 액션 (재개 시 여기부터 — 15회차 종료 시점 기준)
0. **먼저 읽을 3개**: `SettingAgent/docs/20260729_171526_MCP자동바닥ROI_15회차_골든v2와_증폭기통제.md`(15회차 정본) → `docs/20260729_143345_주차면자동생성_14회차후_방향재점검.md`(방향 전환 근거) → `_workspace/00_leader_handoff.md`(확립사실·**반증목록 20건**).
   **마스터 결정 대기**: ①P0 라이브 채점 검증 미이행(재시작 금지로 보류 — 서버 기동해도 되는지) ②②미달 트레이드오프(1:1 −0.019 vs 2:1 +0.078)를 어떻게 볼지 ③A4 픽스처 21MB 커밋 여부 ④실카 선행물 5종(문서 P3 장) ⑤`tools.config.json` 에 남은 154 카메라 평문 비밀번호 처리.
1. (구 항목) `_workspace/00_leader_handoff.md` — 확립사실, 미해결 U 목록, **반증목록(20건)**, 프레임 재캡처 절차.
2. **`_workspace/02k_developer_changes_round11.md` 존재 확인** → 있으면 11회차(스트라이프 규약) 결과부터. 없으면 에이전트 **`dev-roi`** 가 아직 도는 중 — `SendMessage({to:"dev-roi"})` 로 이어서 지시.
3. **12회차 결과에 따라 갈린다 — 핵심 분기:**
   - **드리프트가 추정기 인공물** → 고정 ±22px 창을 **폭 비례**로 바꾸면 해결. **이게 곧 10면 해결**이다
   - **진짜 곡률** → 직선 모델을 버려야 하는 큰 변경. **마스터 판단 필요 — 멈추고 보고**
   - 물리 근거: **핀홀이 평면을 투영하면 직선은 반드시 직선.** 시뮬엔 렌즈왜곡이 없다. 그러므로 휜다면 ①지면 비평면 ②도색이 3D 에서 비직선 ③**추정기 인공물**(유력 — 스트라이프 폭이 7.75→12.50px 로 변하는데 창은 고정 ±22px)
4. 어느 쪽이든 **CLAUDE.md 5대 규칙의 검증(qa)·문서화(documenter) 단계가 미실행**이다. 루프를 접든 이어가든 이 둘은 해야 한다.

## 핵심 결정·가정 (되돌리면 안 됨)
- **f 를 소실점에서 추정하지 않는다.** 카메라 제원(프리셋별 `fov`·`eulerAngles` + 설치고)에서 주입. 실카는 `data/lens_calibration.json` 의 `zoomHfov` 표.
- **카메라 설치고 = 4.950m**(명목 5.0m 아님). 3중 독립 확인.
- **참 주차면 = 2.5×5.0m**(유니티 `preset.list`). 슬롯 치수를 바꾸지 말 것 — 편차 원인은 높이.
- **`aimCenterWeight = 1.6`, 상한 2.2**(3.0 기각). 9회차 24관측 LOPO 로 「검증됨」.
- **DB 쓰기는 `slot.roi.sync` 만.** `slot.roi.load`·`replaceSlotSetup` 은 검출·점유·센터링 전량 소실(실측 23→0).
- **프레임 취득 전 반드시 `roi.show2d {visible:false}`** — 시뮬이 초록 박스를 렌더하며 도색선을 덮는다(평가 오염).
- 마스터 지시로 실행 완료: **슬롯24 제거**(23면), **`idx1` 앵커 교정**. `idx8` 은 **미교정**(증거 부족).

## 앵커 (다시 안 헤매게)
- **인계 정본**: `SettingAgent/_workspace/00_leader_handoff.md` ← 제일 먼저
- 리더 판단 기록: `_workspace/00_leader_{context,decisions,loop1_analysis,loop2_analysis}.md`
- 설계: `_workspace/01_architect_plan.md` / 라운드별: `_workspace/02{,b~j}_developer_changes*.md`
- 공개 설계서: `SettingAgent/docs/20260729_010651_MCP자동_바닥ROI_생성_설계.md`
- 신규 소스: `src/ground/{floorPaint,bayGeometry,bayGrid,roiAutoScore,cameraIntrinsics,placeMetaIntrinsics}.ts` · `src/rpc/services/roiAuto.ts`
- 도구: `src/tools/{roiAutoBench,roiAutoOverlay,roiAutoDiag,anchorCheck,roiAutoRowWhy,roiAutoResidual}.ts`
- 세션 메모: `메모/memo.md` 최상단 항목
- 백그라운드 에이전트: `dev-roi` (SendMessage 로 재개)

## 다음 라운드 착수 항목 (마스터 승인 완료 — 이번 라운드 범위 밖)
**실카 `CameraIntrinsicsProvider` 에 `focalTrue` 보간 적용.** 마스터 제안에서 출발해 리더가 실측 확인:
실카 펌웨어가 보고하는 초점거리가 **최대 7.7% 틀린다**(zoom 3000: focalTrue 2403.53 vs focalFirmware 2588.87).
3회차 전환점이 "f 를 제원에서 주입"이었고 f 오차와 IoU 가 정확히 대응했으므로, 실카에서 이걸 안 고치면 그대로 무너진다.
**곡면율은 쓰지 마라** — `k1`/`k2` 계수가 없고 PTZ 시차 미분리로 미채택 상태다(잔차 31.7~131.2px).
**순서 필수: ① focalTrue 보간 → ② 도색선 직진성 재측정 → ③ 그때도 휘면 곡면율 재캘리브레이션.** 상세는 handoff U15·U16.

## ★ 마스터 판단 대기 (3건)
1. **`1:2` 수동 정본이 5~12% 과대** — s8 +12.4%/+7.6%, s9~s11 +5.4%. 자동은 정확히 2.500/5.000. `1:2` 의 0.739 는 상당 부분 **정본 결함**
2. **`1:3` 축이 정확히 뒤바뀜** — 자동 5.000/2.500 vs 수동 2.553/5.106
3. **`idx8`(=s8) 교정 여부** — 독립 두 측정이 같은 결함 지목(앵커 177px 이탈 + 치수 +12.4%)

## 검증 안 된 것 (은닉 금지)
- **실카(RTSP) 검증 0건.** 환경이 `cameraMode: simulator` 라 불가. 전 수치가 시뮬. **시뮬 수치로 실카 대변 금지**
- **실카 렌즈왜곡 미평가** — 직선이 휘면 VP·직선적합이 무너진다. 시뮬에 없는 고유 위험
- **`roi.auto.apply` 성공경로 미검증** — 임시파일·모킹만. 실정본에 한 번도 실행 안 함
- **DB 고아 행 `slot_id 24`**(cam2:p1#7) 잔존 — 파일에선 제거됐으나 삭제 RPC 가 없다
- **qa-tester·documenter 단계 미실행** — CLAUDE.md 5대 규칙 중 검증·문서화가 아직
- 9회차 24관측은 **전부 시뮬레이터**

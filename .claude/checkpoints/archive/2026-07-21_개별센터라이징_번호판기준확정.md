# 체크포인트: 개별 센터라이징 번호판 기준 확정 — 클릭지점 조준 폐기 (완료·아카이브)
_갱신: 2026-07-21 12:10 · 트리거: 마일스톤(작업 종료·머지·푸시 완료)_

## 목표 (성공기준)
- 실패한 "개별 center"(클릭 **지점** 자체를 화면중앙으로 보내는 patch-NCC 폐루프)를 되돌린다.
- "개별 center+zoom"이 실제로 동작하는지 라이브로 확인한다.
- 동작 확인된 상태를 main에 머지 커밋한다. (추가 지시: 새 작업은 항상 브랜치를 따서 진행)

## 계획·진행
- [x] 진단 — 미커밋 작업 트리에 두 겹이 쌓여 있었음. ②가 "개별 center" UI 슬롯을 점거해 마스터에겐 center가 죽은 것으로 보였다.
- [x] 브랜치 `fix/revert-point-aim` 생성 후 되돌리기
- [x] tsc 0 / vitest 180파일·2095 green
- [x] 라이브 검증 2케이스 정상
- [x] 한글 문서 + 폐기 산출물 이관 + 메모리 갱신
- [x] main `--no-ff` 머지(319b3ff) → GitHub push(0434410)

## 지금 상태
작업 종료. `origin/main` = **0434410** 로 동기(ahead/behind 0). `fix/revert-point-aim` 브랜치는 로컬 보존.
서버 13020은 nodemon 리로드로 되돌린 코드가 이미 반영된 상태.

## ▶ 다음 액션 (재개 시 여기부터)
재개할 잔여 작업 없음. 다음 요청이 들어오면 새 브랜치를 먼저 따고 시작할 것.
미결 1건만 남음 → `SettingAgent/_diag_chord/`(8.2MB 진단 스크래치)와 `SettingAgent/data/refframes/`(런타임 생성 기준 프레임 JPEG)를
`.gitignore`에 추가할지 마스터 확인 필요. 현재는 커밋에서 제외만 해둔 상태(작업트리에 untracked로 남아 있음).

## 핵심 결정·가정
- **개별 센터라이징 정본 = 번호판 기준.** `mode:'plate'`(center, 줌 없음) / `mode:'plate-zoom'`(center + zoomToPlateWidth 0.2). 둘 다 **저장 없음**.
- **`mode:'point'` 폐기 확정** — 라우트 zod enum에서 제거, 요청 시 400. 회귀 가드 테스트로 고정.
- 삭제: `PointAimer.ts`, `patchTrack.ts`, `pointAimer.test.ts`, `patchTrack.test.ts`. `PtzCalibrator.aimPointToCenter` 제거(`centerOnPoint`는 무접촉).
- 화면 중앙 십자 마크(`#center-cross`)는 폐기 겹에서 왔지만 조준 확인에 유용해 **유지**(마스터 미이의).
- 커밋 제외: `_diag_chord/`, `data/refframes/`. 포함: `Sub/HyucomsAPI/`, `etc/HTTP_API_Hucoms_V1.22.pdf`, `data/Place01`, `slot_ptz.json`, `plate_discovery.json`.

## 앵커 (다시 안 헤매게)
- `SettingAgent/src/calibrate/PtzCalibrator.ts` — `centerOnPoint`(번호판 prior `{x,y,w:0,h:0}` → `PlatePtz.centerOnPlate` → 옵션 `zoomToPlateWidth`)
- `SettingAgent/src/api/calibrateRoutes.ts` — `POST /calibrate/point`, `mode: 'plate' | 'plate-zoom'`, 409/400 매핑
- `SettingAgent/web/app.js` — `calPointCenter()`, `overlay.mousedown` 최우선 게이트, 콤보 `#cal-click-mode`
- 문서: `SettingAgent/docs/20260721_120000_클릭지점중앙조준_폐기_되돌리기.md`
- 폐기 산출물: `SettingAgent/_work_old/_workspace_pointaim_20260721_폐기/`
- 라이브 검증 절차(재현용):
  1. `GET /viewer/api/snapshot?cam=1&preset=1&mode=preset` → 현재 화면에서 클릭 좌표 고르기
  2. `POST /calibrate/point {cam,preset,point:{x,y},mode:'plate'|'plate-zoom'}`
  3. `GET /calibrate/frame` 또는 반환 ptz로 `mode=manual` 스냅샷 → 번호판이 중앙인지 육안 대조

## 검증 안 된 것 (은닉 금지)
- 검증은 **cam1/preset1 · 클릭 2점**만 수행. 타 카메라·프리셋, 야간/저조도, 원거리 소형 번호판은 미확인.
- 브라우저 UI에서의 실제 마우스 클릭 경로는 **REST 직접 호출로만 확인**했다. 마스터의 실조작 확인 필요.
  (이번 실패의 교훈이 정확히 이 지점 — 스크립트 검증 통과 ≠ 실사용 동작.)

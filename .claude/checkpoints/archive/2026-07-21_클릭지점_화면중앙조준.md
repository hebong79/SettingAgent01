# 체크포인트: 개별 center = 클릭 지점 화면중앙 조준 (구현·검증 완료, 머지 대기)
_갱신: 2026-07-21 13:40 · 트리거: 마일스톤(구현·라이브검증·문서 완료, 커밋됨)_

## 목표 (성공기준)
"개별 center"를 클릭 **지점** → 화면중앙(pan/tilt only, zoom 불변, 저장 없음)으로 재정의.
휴컴스 `ptz_centering setcenter type=point` 를 참조·활용.

## 지금 상태
브랜치 `feat/click-center-pantilt` 에 커밋 **cbba435** 1개. main 미머지·미푸시.
tsc 0 / vitest 182파일·2136테스트 green. 서버 13020 nodemon 반영 완료.

## ▶ 다음 액션 (재개 시 여기부터)
1. **마스터가 브라우저에서 실제 마우스 클릭으로 확인**(이번 기능의 완료 판정 — 오전 실패의 교훈).
   정밀수집 화면 → `개별 센터라이징` 콤보 = `개별 center` → 라이브뷰 아무 곳 클릭 → 그 지점이 중앙 십자로.
2. 확인되면 main `--no-ff` 머지 → GitHub push.
3. 미결(오전부터 이월): `SettingAgent/_diag_chord/`, `SettingAgent/data/refframes/` 를 .gitignore 에 넣을지 마스터 확인.

## 핵심 결정
- 방안 C(능력 협상 하이브리드): 정본=기하 오픈루프 1샷, 실카=휴컴스 네이티브 위임.
  현재 선택 런타임이 시뮬(`cameraRuntime.selectedCameraId="simulator-1"`)이라 A 단독은 오늘 검증 불가였다.
- 기하는 신규 수학 아님 — `preAimPtz` 와 동일(`controlMath.aimPtzForPoint` 로 추출·공유).
- `개별 center+zoom`(=`plate-zoom`, 번호판 기준)은 무접촉 유지.

## 앵커
- `src/calibrate/controlMath.ts` `aimPtzForPoint` / `src/calibrate/PtzCalibrator.ts` `aimPointToCenter`
- `src/viewer/RealPtzSource.ts` `centerOnPoint`(0~1 → 1920x1080 픽셀) / `src/clients/CameraSourceClient.ts` 조건부 할당
- `src/api/calibrateRoutes.ts` `mode:'point'|'plate'|'plate-zoom'` / `web/app.js` `calPointCenter`
- 문서: `SettingAgent/docs/20260721_132500_클릭지점_화면중앙조준_능력협상.md`, 산출물 `SettingAgent/_workspace/00~04`

## 검증 안 된 것 (은닉 금지)
- 실카메라(192.168.0.153) 네이티브 경로: 장비 미선택 → 라이브 미검증(와이어 형식까지만).
- 1920×1080 픽셀 기준 가정, 게인은 cam1 실측치(타 카메라 일반화 미확인).
- 브라우저 실제 마우스 클릭 경로는 REST 직접 호출로만 확인.
- QA 잔여 권고 1건 미반영(범위 밖): 응답 `mode`(native/geometric)를 UI에 표시 안 함.

## 종료 (2026-07-21 13:50)
마스터 실사용 확인 → main `--no-ff` 머지(103440d) → origin/main push 완료. 재개할 잔여 작업 없음.
미결 1건 이월: `_diag_chord/`·`data/refframes/` .gitignore 여부 마스터 확인 필요.

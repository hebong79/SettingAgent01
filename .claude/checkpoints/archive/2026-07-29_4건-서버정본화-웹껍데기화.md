# 체크포인트: 4건 서버 정본화(토큰·투어링·점유판정·슬롯편집) + 웹 껍데기화
_갱신: 2026-07-28 23:30 · 트리거: 마일스톤(구현 4웨이브 완료·라이브 검증 중 결함 후보 발견)_

## 목표 (성공기준)
마스터 요청: "토큰, 투어링, 점유판정, 슬롯편집 순으로 작업하고 **4개 모두 서버에 적재**하여 **웹은 껍데기로만 연동**할 수 있게".
memo.md 재개지점 표 우선순위 1~4 와 동일. 성공 = 4건이 서버 REST+RPC 로 호출 가능하고, 웹은 계산/판정 로직 없이 서버 결과만 렌더. tsc 0 · vitest 회귀 0 · 한글 문서·영향도 분석.

## 작업 위치
```
워크트리: D:\Work\Parking3D\AgentVLA\ParkAgent\.claude\worktrees\feat-server-promote-4
브랜치:   worktree-feat-server-promote-4  (main 19d5ad2 에서 분기)
커밋:     아직 0건 — 전량 미커밋 작업본
```

## 계획·진행
- [x] Phase 0·1 — 메모/체크포인트 확인, 워크트리 진입, 설계자(opus) 설계서
- [x] 리더 판단 Q1~Q4 확정 → `_workspace/00_leader_decisions.md`
- [x] W1 인증토큰 — `src/api/controlGate.ts`(deny-by-default 전역훅) + `web/token.js`(localStorage·`mutFetch`) + 드리프트 테스트. 3291 pass
- [x] W2 투어링 — `src/setup/touringPlan.ts`·`src/capture/TourJob.ts`·`src/api/tourRoutes.ts`. 3349 pass
- [x] W3 점유판정 — `src/domain/occupancyJudge.ts` + `POST /capture/slots/judge-occupancy`. 3396 pass
- [x] W4 슬롯편집 — `src/setup/artifactSlotEdit.ts` + `/mapping/slot/add|delete`(`dryRun`). 3436 pass
- [x] 리더 라이브 검증(13021) — 카탈로그 76·토큰게이트·dryRun md5 불변·점유 웹↔서버 완전일치
- [~] **투어링 라이브 결함 후보 조사** — dev-w2-tour 에게 지시함(아래 ▶)
- [~] QA 독립 교차검증(qa-promote4) · 문서화(doc-promote4) 백그라운드 진행 중
- [ ] 최종 종합 보고 · memo.md 세션 기록 · 커밋

## 지금 상태
구현 4건 전부 완료, `npx tsc --noEmit` 0 / `npx vitest run` **2 failed | 3436 passed**(실패 2건은 사전 존재: `roiDbLoad`·`placeRoiRuntimeInvariants`).
라이브(13021) 검증에서 대부분 통과했으나 **투어링에서 성공 위장 의심** 발견: 28/28 done·skipped=0 인데 같은 구간 로그의 실제 카메라 이동 명령은 **1건뿐**이고 흡수 warn 도 0건.

## ▶ 다음 액션 (재개 시 여기부터)
1. `dev-w2-tour` 의 투어링 조사 결과 확인 → 보고 품질 결함이면 `succeeded`/`failed` 카운터 노출 수정 + 음성 대조 테스트 확인.
2. `qa-promote4`(교차검증)·`doc-promote4`(한글 문서 2종) 결과 수령·검토.
3. **환경 원복 확인**: `config/tools.config.json` port 13020·`controlToken:""`, `save/setup_result.json`(내가 복사한 fixture) 삭제, 13021 서버 종료.
4. 전체 `tsc`+`vitest` 재확인 → 커밋 → 마스터 보고 → `메모/memo.md` 최상단에 세션 기록.

## 핵심 결정·가정 (되돌리지 말 것)
- **RPC 는 로직을 갖지 않는다** — 신규 6 메서드 전부 `app.inject()` 위임. `test/rpcParity.test.ts` 양방향 고정 유지(+W2 가 T4 동적 등록검사 추가).
- **Q3**: `/mapping/slot/*` 에 `dryRun`(기본 false). 웹은 `dryRun:true`+버퍼 전달로 **기존 2단계 UX 보존**, 외부 RPC 는 한 방에 커밋. 서버 정본화·웹 껍데기화·UX 유지 셋 다 만족.
- **Q1**: `POST /capture/detect` 게이트 면제 현행 유지(읽기 선언인데 카메라를 움직이는 모순은 **별건·문서화**).
- **Q4**: `setup.slot.add` 는 **artifact 편집**이지 주차면 추가가 아니다(주차면 추가는 `place.space.add`+`slot.roi.sync`).
- **DB 쓰기 0** — `replaceSlotSetup` 계열 미호출(검출·센터링 23→0 파괴 전례 회피). `sqlite` 는 읽기만.
- 파리티 **기준변은 항상 `web/*`**. 웹 원본 함수는 기준변이라 **삭제 금지**(존치하되 런타임 미호출).
- **음성 대조 규약(§9.2)**: 봉인 테스트는 "실제로 실패하는 입력"을 한 번 보여주고 원복한다. green 이 곧 봉인 작동은 아니다(W2 의 R15 로 실증됨).

## 앵커
- 산출물: `SettingAgent/_workspace/` — `00_leader_decisions.md` · `01_architect_plan.md` · `02_developer_changes.md`(W1) · `02b_..._tour.md` · `02c_..._occupancy.md` · `02d_..._slotedit.md` · `03_qa_report.md`(진행중) · `04_doc_impact.md`(진행중)
- 신규 소스: `src/api/controlGate.ts` · `src/setup/touringPlan.ts` · `src/capture/TourJob.ts` · `src/api/tourRoutes.ts` · `src/domain/occupancyJudge.ts` · `src/setup/artifactSlotEdit.ts` · `web/token.js`
- 라이브 로그: `<scratchpad>/server13021.log` · artifact 백업 `<scratchpad>/artifact.orig.json` · config 백업 `<scratchpad>/tools.config.backup.json`
- `GET /rpc/catalog` = **76**(70→76: tour 3 + occupancy 1 + slot 2)

## 검증 안 된 것 (은닉 금지)
- **투어링 실제 카메라 순회** — 28/28 done 인데 이동 명령 1건. 조사 진행 중. **성공으로 처리하지 말 것.**
- **라이브 브라우저 육안 확인 전무** — 투어링 폴링 UX·점유 오버레이(원/사다리꼴/뱃지)·슬롯편집 2단계 UX 전부 코드/정적 테스트로만 확인.
- 실카(Hucoms 192.168.0.153) 환경에서 `getptzfpos` 8건 fetch 실패 — 원인 미규명(이번 변경과 무관할 가능성 높으나 확인 안 함).
- 사전 실패 2건은 무접촉·상태 변화 없음(마스터 판단 대기 항목).

# 27-A 구현 보고 — 콘투어 정제 (K1 최종 판정)

- 작성: 2026-07-31 / 구현자 · 브랜치 `main` 직접(워크트리 생성 없음) · 커밋 안 함(`git add -A` 금지 준수)
- 지시서: `_workspace/51_architect_plan_round27_contour_refine.md`
- **이 문서의 모든 수치는 원시 배정도**다. `toFixed` 는 SVG 좌표·스샷 헤더 문자열에만 썼다.
- 카메라 미사용 — 골든 JPEG + `reports/detcache_r24/` 캐시만. `--fetch` 미실행.

---

## 0장. ★ K1 최종 판정 — **T4. 미달. 선택지 B 상신.**

### 0-1. 판정

> **T4 — 「하단 콘투어의 선분 모델에 방위 정보가 없다」 확정 → K1 확정 · 선택지 B 1회차 시험 상신.**

근거값 3개(전부 **계측 전용 오라클 상한** — 실검출이 아니다):

| 상한 지표 | 값 | 판정선 | 결과 |
|---|---|---|---|
| best-of-two(원 콘투어) | `11.100430381019102` | ≤ `10` | **초과** |
| **best-of-two(F4 정제 후)** ← T4 근거값 | `11.100430381019102` | ≤ `10` | **초과** |
| best-of-three(3선분) | `11.007123357580006` | ≤ `10` | **초과** |

**두 선분 중 최선을 신이 골라줘도, 세 선분으로 늘려서 신이 골라줘도 10° 를 못 넘는다.** 선분을 하나 더 준 대가는 중앙값 `0.09330702343809616`° 다.

### 0-2. 실패·미달·미측정 — 먼저

| # | 항목 | 실측 |
|---|---|---|
| **F-1** | **Q1 미달** | F4 정제본 방위 중앙값 `13.801162888055714` > `10`. **26회차 종점과 완전히 같다(이득 = `0`)** |
| **F-2** | **Q2 미달** | `--edge kink2` recall `0.2926829268292683` ≤ `0.5853658536585366`(필요) · precision(게이트 후) `0.375` |
| **F-3** | **★ F4 는 「원인 수리」가 아니다** | Q9 음성 대조군 **N1 무작위 절사 이득 `0.5983810637389073` > F4 이득 `0`**. 대조군이 수리를 이겼다. 규약대로 **「원인 수리」라고 쓰지 않는다** |
| **F-4** | **★ F4 는 원리적으로 방위를 못 바꾼다(설계 결함 · 사후 판명)** | 꺾임점 고정은 끝점을 **같은 TLS 직선 위에서** 옮긴다 → 앞변 **방향이 불변**이다. `footprintFromContact` 는 방향(`unit3(B−A)`)으로 quad 를 만들므로 방위가 바뀔 수 없다. 실측으로도 kink2 ≡ kink (p75 만 `24.479186172246155` → `24.479186172246123`, 부동소수 오차) |
| **F-5** | **ⓒ 미분해** | 설계 §2 의 C4b 지표 `(chordLenM/4.7)/mpp` 가 **퇴화**했다 — 36건 전부 `19.57` ~ `175.32`, 중앙값 `82.12765957446808`. 「1 을 크게 넘는 마스크」가 전체집합이 되어 **판별력이 0**이다. 채택 조건도 기각 조건도 충족 불가 → **미분해** |
| **F-6** | 미측정 | 지면 꺾임각이 90° 가 아닌 이유(설계 §10-4)는 **여전히 미분해**. C4b·C5 가 부분 답만 줬다 |
| **F-7** | 미측정 | 실카 σ · 실카 tilt 분포 — 27-A 는 카메라를 안 쓴다(27-B·27-C 소관) |

### 0-3. ★ W3 의 (i):(ii) 비 — 사격 방향의 1차 결정자

> **(i) 선택 오류(`ruleOk=false`) `5` : (ii) `ruleOk=true` 인데 악화 `9`** → **(ii) 「분해 자체」 우세**

그리고 더 결정적인 수치:

> **악화 14건 중 `12` 건은 두 선분 **다** chord 보다 나쁘다**(`min(angA,angB) > angChord`).

즉 악화의 정체는 「좋은 쪽을 놓쳤다」가 아니라 **「고를 만한 것이 애초에 없었다」**다. 이것이 §3 F0(선택 규칙 교체)를 쏘지 않은 실측 근거다.

---

## 1. 단계 0 — 스냅샷 + 앵커 3종 (T0 회피 확인)

### 1-1. git 스냅샷 (`_workspace/_r27a_gitstatus_before.txt`)

```
 M SettingAgent/src/rpc/services/roiAuto.ts          ← 27-B
?? SettingAgent/_workspace/51_architect_plan_round27_contour_refine.md
?? SettingAgent/src/capture/frameArchive.ts          ← 27-C
?? SettingAgent/src/tools/realGroundSplit.ts         ← 27-C
```

작업 중 27-B·27-C 가 `SettingAgent/vitest.config.ts`(M) · `SettingAgent/src/tools/roiAutoReplay.ts`(??) 를 추가했다. **전부 무접촉.**

### 1-2. 캐시 확인

`reports/detcache_r24/` 10파일 · frameHash 5개 `6006a034bfe2`/`ceaaed722663`/`3c0db12efe75`/`e33628e921c2`/`0cf4fda4d3aa` · 마스크 36 · 판 33. `--fetch` 미실행.

### 1-3. 앵커 — **전부 비트 동일 (T0 아님)**

| 앵커 | 기대 | 실측 | 판정 |
|---|---|---|---|
| **Q3** `--edge chord` | recall `0.04878048780487805` · precision `0.0625` · 산출 32 · 회수면 2 | 동일 | **비트 동일** |
| Q3 방위(chord) | 중앙값 `31.052940498468203` · \|dy\| `101.66977056400071` | 동일 | **비트 동일** |
| **Q3b** `--edge kink` | recall `0.2926829268292683` · precision `0.36363636363636365` · 산출 33 · 회수면 12 | 동일 | **비트 동일** |
| Q3b 방위(kink) | 중앙값 `13.801162888055714` · best-of-two `11.100430381019102` · \|dy\| `47.16350014200509` | 동일 | **비트 동일** |
| **Q4** `--source degraded` | 산출 `42` · 회수면 `39` · recall `0.9512195121951219` · precision `0.9285714285714286` · 미회수 「검출미탐 1 · 기하결손 1」 | 동일 | **비트 동일** |

**출발점이 26회차 종점과 같다는 것이 확인됐다.** 이후 모든 수치는 이 출발점 위의 값이다.

### 1-4. Q8 red 기준선 스냅샷

착수 시점 `npx vitest run` = **306파일 / 3874테스트 · red 1건**:
`test/roiAutoHoldout.test.ts > roi.auto 가 쓰는 ground 모듈은 전부 봉인 대상이거나 명시적 예외다`
(`src/ground/types.ts 가 roi.auto 경로에 들어왔는데 …` — **27-B 의 `src/rpc/services/roiAuto.ts` 수정 유래**. 내 책임 아님.)
26회차에 red 였던 `test/gridDiagWiring.test.ts` 2건은 착수 시점에 이미 green 이었다.

---

## 2. 단계 A — 부검 원장 (수리 코드 0줄)

산출: **`_workspace/_r27a_ledger.tsv`**(헤더 1줄 + 36행 · 25열 · 원시 배정도) · 집계 `_workspace/_r27a_W.txt`.

### 2-1. 분류 검증

**개선 `16` · 악화 `14` · 폴백 `6`** — §44 §2 와 **정확히 일치**. 분류 정의 대조 불필요.

### 2-2. W1 — 악화군 vs 개선군 속성별 중앙값 대조

판독 규약(사전 고정): 중앙값 비 ≥2배(또는 ≤0.5배) **이거나** 한 군 중앙값이 상대군 [min,max] 밖일 때만 「분리된다」.

| 속성 | 악화 median | 개선 median | 비 | 개선군 [min,max] 밖? | **판정** |
|---|---|---|---|---|---|
| **angChord** | `10.13392870217486` | `54.15659393448079` | `0.18712271149169743` | false | **분리된다** |
| cols | `148.5` | `101.5` | `1.4630541871921183` | false | 분리 안 됨 |
| kinkDeg | `46.62512363592769` | `46.67503847162473` | `0.9989305882259233` | false | 분리 안 됨 |
| gain | `2.719570752121408` | `4.229421772439071` | `0.6430124254439289` | false | 분리 안 됨 |
| kFrac | `0.4540679908326967` | `0.2949810606060606` | `1.5393123541551452` | false | 분리 안 됨 |
| cleanRatio | `0.6340406719717064` | `0.8700897867564534` | `0.7287071766872498` | false | 분리 안 됨 |
| depthM | `18.02449172524915` | `23.90215535447672` | `0.7540948277651153` | false | 분리 안 됨 |
| mpp | `0.008565320409189024` | `0.009285823831439857` | `0.9224082391256058` | false | 분리 안 됨 |
| areaPx | `99450` | `59157` | `1.6811197322379432` | false | 분리 안 됨 |
| tilt | `17` | `17` | `1` | false | 분리 안 됨 |

> ★ **분리되는 속성은 `angChord` 하나뿐이고, 그것은 동어반복에 가깝다** — 「악화군 = chord 가 원래 잘 맞던 군」. 오염(cleanRatio)·원경(depthM·mpp)·크기(areaPx)·틸트 **어느 것도 악화군을 가르지 못한다.** 이것 자체가 「정제할 대상이 실측으로 존재하지 않는다」의 1차 신호다.
> ⚠ W1 은 상관이며, 인과로 쓰지 않았다. §2 측정의 우선순위만 정했다.

### 2-3. W2 — 악화 14건의 `angChord` 분포

```
n=14 min=0.2704011496752948 p25=1.543788953141434 median=10.13392870217486
      medianLow=9.313506419048432 p75=14.526904368216895 max=61.373257603237484
```

**중앙값 `10.13392870217486` ≥ `10` → F0 발동 조건(「< 10」) 미충족.** 사전 고정 문턱을 `0.13392870217486` 차이로 넘지 못했다. **문턱을 사후에 조정하지 않았다.**

### 2-4. W3 — 악화 2분 (★ 1차 결정자)

| 분류 | 건수 | 의미 |
|---|---|---|
| (i) `ruleOk=false` (선택 오류) | **5** | R3 가 best 를 놓쳤다 |
| (ii) `ruleOk=true` 인데 악화 | **9** | 옳게 골랐는데도 나빠졌다 |
| **두 선분 다 chord 보다 나쁨** | **12 / 14** | **고를 만한 것이 없었다** |

→ **(ii) 우세 → 「분해 자체」가 병목 → §2 측정 순서 `C3 → C1 → C6 → C4b → C5`.**

악화 상세(dAng 내림차순, 상위 5):

| 대상 | angChord → angKink | dAng | ruleOk | kFrac | cleanRatio | depthM |
|---|---|---|---|---|---|---|
| 1:1#2 | `5.008592546841585` → `37.38676403475789` | `32.378171487916305` | false | `0.4666666666666667` | `0.7777777777777778` | `27.544627626529675` |
| 2:1#3 | `11.296604723826004` → `38.93688683683247` | `27.640282113006467` | false | `0.3563218390804598` | `0.632183908045977` | `29.477820323249677` |
| 2:2#7 | `61.373257603237484` → `85.6313443516677` | `24.258086748430223` | false | `0.27848101265822783` | `1` | `31.705860813732286` |
| 1:2#2 | `39.34977119442373` → `63.322967723387514` | `23.973196528963783` | false | `0.44385026737967914` | `0.43315508021390375` | `15.163049666538354` |
| 1:1#6 | `1.543788953141434` → `24.51951615696679` | `22.975727203825357` | false | `0.4642857142857143` | `0.5952380952380952` | `28.31841807620175` |

### 2-5. W4 — 폴백 6건 제외 후 30건

방위 중앙값 kink `12.482965006828001` · chord `31.052940498468203` · 개선 16 : 악화 14.
폴백을 빼도 **`10` 을 못 넘는다**. 폴백 혼입은 통계를 흐리는 주범이 아니다. (판정선 아님 — 판정은 36건 전수.)

---

## 3. 단계 B — 판별 측정 C1~C6 → ⓐ~ⓔ 판정

**모든 채택/기각 조건은 설계 §2 표에 사전 고정된 것이며, 사후에 바꾸지 않았다.**

| 후보 | 측정 | 실측 | **판정** |
|---|---|---|---|
| **ⓐ 오염(타차 가림)** | **C1** 가림배제 콘투어로 `frontEdgeOf` 재실행 | 중앙값 `13.801162888055714` — 기준 대비 **차 `0`**. cleanRatio 낮은군 이득 median `0.6889889925164043`(n=18) / 높은군 `0`(n=18) | **기각** (중앙값 유의 하락 없음) |
| **ⓐ 오염(그림자)** | **C2** `dyKink` 부호 있는 중앙값 | `-38.51197117081026` · **양수 2/36 · 음수 34/36** | **기각** — 그림자 누출이면 콘투어가 접지보다 **아래**(+)여야 하는데 34/36 이 **위**(−)다. **방향이 반대다** |
| **ⓑ 3선분 필요** | **C3** best-of-three | `11.007123357580006` ≥ `10` · gain3 median `1.6653654378157943` | **기각** — 그리고 이것이 §4 **B 상신 근거** |
| **ⓒ 마스크 병합** | **C4/C4b/C4c** | 4.7m 초과 `8/36`(그중 악화 6) · C4c 겹침쌍 `39` · **C4b 지표 퇴화**(전 36건 ≫1) | **미분해** (F-5) |
| **ⓓ 곡률** | **C5/C5b** | 잔차 부호 런 median segA `4` · segB `5` (>3 = 런 많음 = 직선+노이즈) · C5b kFrac 이동 median `0.004099935364237417`, **악화군만 `0.002761172911360632`(더 작다)** | **기각** — 분할점은 안정적이고 곡선 신호 없음 |
| **ⓔ 스팬 과연장** | **C6** 앞변 끝점 ↔ 꺾임점 최소거리(px) | 중앙값 `14.944379128562943` (0 아님) · **악화군 `30.664152241448438` > 개선/폴백군 `13.63107017901973`** | **채택** ← **유일한 채택** |

### 3-1. C3 상세 (T4 근거 · 별도 절은 §5)

| 지표 | 값 |
|---|---|
| gain3 = rms2/rms3 | `n=36 min=1 p25=1.408587483547644 median=1.6653654378157943 p75=2.0693042757352615 max=6.396187045136584` |
| best-of-two 방위(상한) | `median=11.100430381019102 min=0.4615819290870839 max=43.82039677953849` |
| best-of-three 방위(상한) | `median=11.007123357580006 min=0.23858641571080877 max=45.17052225439121` |

**선분을 2 → 3 으로 늘린 이득은 중앙값 `0.09330702343809616`° 이고, 최댓값은 오히려 `43.82039677953849` → `45.17052225439121` 로 악화한다.**

### 3-2. C4 상세 (미분해 사유)

4.7m 초과 8건: `1:1#2`(L `7.436981034531871`, pxW 356, mpp `0.02089039616441537`, tilt 8.7, 악화) · `1:1#6`(L `7.901097038562919`, pxW 332, mpp `0.023798485055912408`, tilt 8.7, 악화) · `1:2#6`(L `7.101029332898494`, pxW 632, mpp `0.01123580590648496`, tilt 20.1, 개선) · `2:1#0`(L `6.1978243111387465`, pxW 380, mpp `0.016310063976680913`, tilt 10, 개선) · `2:1#3`(L `9.88114071483556`, pxW 692, tilt 10, 악화) · `2:2#2`(L `7.030452515611537`, pxW 776, tilt 17, 악화) · `2:2#4`(L `7.773264259241794`, pxW 668, tilt 17, 악화) · `2:2#6`(L `6.698847150113862`, pxW 752, tilt 17, 악화).

- **기각 조건 미충족**: 「전부 저틸트(1:1·2:1)」가 아니다 — 8건 중 4건이 tilt 17~20.1 이다.
- **채택 조건 판정 불가**: C4b 가 퇴화해 부분집합을 못 만든다.
- 원시 성분으로는 `1:1#2`·`1:1#6`·`2:1#0` 은 **고 mpp / 저 픽셀폭 = 원경 확대**, `2:2#2`·`2:2#4`·`2:2#6`·`2:1#3`·`1:2#6` 은 **고 픽셀폭 / 평범한 mpp = 병합 후보**로 갈린다. **다만 이것은 사전 고정 지표가 아니므로 판정으로 쓰지 않는다.**

---

## 4. 단계 C·D — 사격 결과 (F4 1발)

### 4-1. 무엇을 쏘았고, 무엇을 쏘지 않았나

| # | 수리 | 발동 조건 | 충족? | 조치 |
|---|---|---|---|---|
| **F0** 선택 규칙 교체 | W3 (i) 우세 **또는** W2 `angChord` median < 10 | (i)5 < (ii)9 **AND** `10.13392870217486` ≥ 10 | **미충족** | **쏘지 않음** — 사유는 §4-3 |
| **F1** 가림열 배제 | C1 채택 | C1 이득 `0` | **미충족** | 쏘지 않음 |
| **F2** 원경 꼬리 절사 | C4b 「원경 확대」 확인 | C4b 퇴화 | **미충족** | 쏘지 않음 |
| **F3** 3선분 | best-of-three < `10` | `11.007123357580006` | **미충족** | 쏘지 않음 |
| **F4** 꺾임점 고정 | C6 채택 | **충족** | **쏘았다(1발)** | 아래 |

> 설계 §10-2 · 리더 판정 2 를 따랐다 — **발동 조건 미충족인 수리를 억지로 만들지 않았다.** 채택된 것은 ⓔ 하나뿐이므로 사격도 1발이다.

### 4-2. F4 결과 — Q1 / Q2 / Q9

| 지표 | 26회차 kink | **27-A F4 (kink2)** | 이득 | 판정선 |
|---|---|---|---|---|
| **Q1 방위 중앙값** | `13.801162888055714` | **`13.801162888055714`** | **`0`** | ≤ `10` → **FAIL** |
| Q1 개선:악화 | 16:14 | **16:14** (악화 ≤ 개선) | — | 부수 조건은 만족 |
| p25 / p75 / max | `5.191295854090351` / `24.479186172246155` / `85.6313443516677` | `5.191295854090351` / `24.479186172246123` / `85.6313443516677` | 부동소수 오차만 | — |
| **Q2 recall(실검출)** | `0.2926829268292683` | **`0.2926829268292683`** | `0` | > `0.5853658536585366` → **FAIL** |
| Q2 precision | `0.36363636363636365` | 게이트 후 `0.375` (산출 33 → 배타성 후 32) | — | ≥ `0.70` → **FAIL** |

**Q9 음성 대조군 (같은 실행에서 산출 — frameHash 물리적 동일)**

| 대조군 | 방위 중앙값 | kink 대비 이득 |
|---|---|---|
| **F4 (수리)** | `13.801162888055714` | **`0`** |
| **N1 무작위 절사**(시드 = key 해시 · 지운 열 수 median `20`) | `13.202781824316807` | **`0.5983810637389073`** |
| ⓒ 근측밴드 12px(26회차) | `24.140026999999996` | `-10.338864111944282` |

> ★ **N1 이 F4 를 이겼다.** 규약(§3-1)에 따라 **F4 를 「원인 수리」라고 쓰지 않는다.**
> N1 을 F1/F2 미발동에도 불구하고 산출한 이유: 리더 지시가 필수로 지정했고, 「열을 지우면 뭐든 좋아진다」의 크기(`0.598`°)를 정량화해 두면 B 회차가 같은 함정을 재방문하지 않는다.

### 4-3. ★ F0 를 쏘지 않은 이유 — 「1회 잔여」를 태우지 않았다

리더가 F0 1회 사용을 승인했으나 **발동 조건이 미충족이고, 더 중요하게는 실측으로 상한이 판정선 아래다**:

| 규칙 | best-of-two 일치율 | 채택 시 방위 오차 median |
|---|---|---|
| R1 길이(짧은 쪽) | `23/36 = 0.6388888888888888` | **`13.989642795383059`** |
| R2 하단(화면 아래) | `26/36 = 0.7222222222222222` | **`14.154963029704813`** |
| **R3 시선직교(현행)** | `28/36 = 0.7777777777777778` | **`11.100430381019102`** |

**R3 → R2 또는 R1 로 바꾸면 오차가 커진다.** 그리고 어떤 선택 규칙도 best-of-two `11.100430381019102` 를 넘을 수 없는데, **그 상한 자체가 `10` 을 넘는다.** 따라서 F0 는 「쏘면 실패할 것이 사전에 계산되는 사격」이며, 쏘는 것이 곧 「억지 수리」다(설계 §10-2). **F0 1회는 소진하지 않았다 — B 회차로 이월 가능하다.**

### 4-4. ★ F4 가 왜 0 이었나 (사후 판명한 설계 결함)

`segEndpoints` 는 조각을 **TLS 직선 위로 투영**해 min/max t 를 취한다. F4 는 그 두 점 중 하나를 **같은 직선 위의 다른 점**(꺾임점의 투영)으로 바꾼다. 즉 **선분의 방향 벡터가 불변**이다. 그런데 `footprintFromContact`(`imageObservation.ts:111`)는 `side = unit3(B−A)` 로 quad 를 세우므로 **방위는 방향에만 의존**한다.

→ **ⓔ(스팬 과연장)는 실재하지만(C6 확정: 악화군 `30.66` px vs 개선군 `13.63` px), 그것은 방위 오차를 운반하지 않는다.** 스팬은 `groundAxisOf` 의 장축 선택이 뒤집힐 때만 방위에 영향을 주는데, kink 모드에서 4.7m 초과는 이미 `1/36` 로 줄어 있었다(26회차).

**이것은 설계 §3 F4 항의 잠재 결함이다.** 설계 §2 ⓔ 의 판별 측정 C6 은 「스팬이 과연장되는가」를 정확히 쟀지만, 「과연장이 방위에 영향을 주는가」는 묻지 않았다. 사격 전에 이 한 줄을 확인했다면 F4 도 쏘지 않았을 것이다. 다만 결과적으로 **T4 판정은 F4 와 독립**이다 — best-of-N 상한이 이미 판정선을 넘었기 때문이다.

---

## 5. ★ Q10 — best-of-N 상한 (T4 판정의 근거값 · 별도 절)

> **아래는 전부 「계측 전용 오라클 상한」이며 실검출 성능이 아니다.** 강등본 정답으로 최선의 선분을 골라 준 가상의 값이다. 실제 파이프라인은 이 값에 도달할 수 없다.

| 구성 | 방위 오차 중앙값 (상한) | 판정선 `10` |
|---|---|---|
| best-of-two — 원 콘투어 | `11.100430381019102` | 초과 |
| **best-of-two — F4 정제 후** | **`11.100430381019102`** | **초과** ← T4 근거값 |
| best-of-three — 3선분 | `11.007123357580006` | 초과 |
| (참고) 클램프 후 best-of-two | `12.482965006828001` | 초과 |

**대비 — 실검출(상한 아님):**

| 구성 | 방위 중앙값(실검출) | vpdseg recall(실검출) |
|---|---|---|
| chord (24회차) | `31.052940498468203` | `0.04878048780487805` |
| kink (26회차) | `13.801162888055714` | `0.2926829268292683` |
| **kink2 (27-A F4)** | **`13.801162888055714`** | **`0.2926829268292683`** |
| (참고 · 도달 불가 천장) seg 상한 | — | `0.8780487804878049` |

**T4 성립:** Q1 `13.801162888055714` > `10` **AND** 정제 후 best-of-N `11.100430381019102` > `10`.

---

## 6. 단계 E — 스샷 · 육안 4문

산출 디렉터리 **`reports/overlay_r27a/`** (13장):
`r27_pre_{cam}_{preset}_{frameHash}.png` 5장(26회차 kink 재현 = Q3b 앵커 상태) · `r27_post_…` 5장(F4 정제 후) · 확대본 3장.

**악화 대표 3건 — 원장 `dAng` 내림차순 자동 선정(사람이 고르지 않았다):**

| 순위 | 대상 | dAng | 파일 |
|---|---|---|---|
| 1 | 1:1 seg#2 | `32.378171487916305` | `r27_worse1_1_1_6006a034bfe2_seg2.png` |
| 2 | 2:1 seg#3 | `27.640282113006467` | `r27_worse2_2_1_e33628e921c2_seg3.png` |
| 3 | 2:2 seg#7 | `24.258086748430223` | `r27_worse3_2_2_0cf4fda4d3aa_seg7.png` |

확대본은 지시대로 콘투어를 **「점」**으로 그렸다(segA `#40c4ff` / segB `#ffab40` · 분할점 흰 ★ + `k`·`kFrac` 라벨 · 가림배제 탈락열 회색 ✕).

### 6-1. 육안 4문 답변

> ⚠ 육안은 수치의 보조다. 아래는 §2 판별 측정의 **해석 보조**로만 인용한다.

**① ⓐ — 콘투어 점이 차체 접지선 위에 있는가?**
**아니다. 다수의 열에서 접지선이 아니다.** 악화#1(1:1#2)·악화#2(2:1#3)에서 segA(파랑) 점들이 **앞차의 옆면 실루엣을 따라 대각선으로 올라간다** — 지면이 아니라 차체 측면 경계다. 전체 프레임 `r27_post_2_2_…` 에서는 청록 `bottomContour` 폴리라인이 **차 지붕 위까지 타고 올라가는 것**이 명백히 보인다. 벗어나는 구간은 **앞차에 가려진 좌측 열 구간**이다.
※ 이것이 C1(가림배제)로 안 잡히는 이유: `bottomContour` 는 열별 **마스크 폴리곤의 최하단 점**을 취하는데, 가려진 열에서 그 점은 **자기 마스크 안의 차체 위**에 있다. `rejectOccluded` 는 「점 아래 6px 이 **다른** 마스크 안인가」만 보므로 원리적으로 못 잡는다(`contact.ts:140` 주석이 이미 명시한 한계).

**② ⓑ/ⓓ — 두 직선인가, 세 구간인가, 연속 곡선인가?**
**셋 다 아니다.** 악화#1·#2 는 「**대각 실루엣 런 + 산개한 하단 런**」이다. 흰 ★ 는 육안으로 보이는 꺾임 위치에 **대체로 맞게** 놓여 있으나, 그 꺾임이 가르는 것은 「옆면 접지선 ↔ 앞범퍼 접지선」이 **아니라** 「실루엣 경계 ↔ 하단」이다. 선분 수를 늘려도(C3) 이득이 없는 이유가 여기서 보인다.

**③ ⓒ — 하나의 마스크 안에 차 두 대의 접지선이 보이는가?**
**악화#2(2:1#3)에서 그렇게 보인다** — 콘투어 점이 좌측 은색 차의 측면과 중앙 검정 차의 하단에 **걸쳐 있다**. 다만 이것이 「마스크 병합」인지 「가림으로 인한 실루엣 추종」인지는 **육안으로 갈리지 않으며**, C4b 가 퇴화해 수치로도 못 갈랐다(F-5 · ⓒ 미분해).

**④ ⓔ — 자홍 앞변의 꺾임점 쪽 끝이 ★ 에 붙어 있는가?**
**악화#3(2:2#7)에서는 명백히 넘어가 있다** — 자홍 선이 ★ 를 지나 오른쪽으로 길게 연장된다. 악화#1 에서는 비교적 짧게 ★ 근방에 있다. 즉 **ⓔ 는 실재하고 육안으로도 보인다**(C6 수치와 일치). 그러나 §4-4 대로 그 과연장은 **방향을 바꾸지 않으므로 방위 오차와 무관**하다.

---

## 7. 단계 F — 회귀 · 봉인 (Q6~Q8)

| # | 항목 | 실측 | 판정 |
|---|---|---|---|
| **Q6** | 봉인 — `contourRefine.ts` 에 `faceSlot`·`presetId`·`visible`·`rotY`·`pos.`·`t.vis`·`degradeCar`·`simDegradedSource`·`carList` 부재 · `fitContactLine`·`buildFootprint`·`buildFrameCuboids`·`SlotAxes`·`slotPolysPx` 부재 · `imageObservation.ts` 에 `bottomContour` 존재 | `test/contourRefine.test.ts` 4·5번 green | **PASS** |
| **Q7** | rows 골든 무회귀 — CR 정규화 후 md5 | **`d0af19b395edca366296cbd154f104f5`** | **PASS(동일)** |
| **Q8** | `npx tsc --noEmit` | **오류 0** | PASS |
| **Q8** | `npx vitest run` | **307파일 / 3880테스트 · red 1건** = `test/roiAutoHoldout.test.ts`(단계 0 스냅샷과 **동일 건**) → **신규 red `0`** | **PASS** |
| 봉인 테스트 | `test/imageObservation.test.ts` 7개 · `test/contactOrient.test.ts` 6개 | **무변경 green** | PASS |
| Q4 재확인 | 모든 변경 후 `--source degraded` | `42`/`39`/`0.9512195121951219`/`0.9285714285714286` · **비트 동일 PASS** | **PASS** |
| Q5 상한 정합 | vpdseg recall `0.2926829268292683` ≤ `0.8780487804878049` | 초과 없음 | PASS(T6 아님) |

테스트 수 3874 → 3880 (+6 = `test/contourRefine.test.ts` 신규 6항목).

---

## 8. 판정 표 총괄 (Q1~Q11 · 원시 배정도)

| # | 조건 | 문턱 | 실측 | 판정 |
|---|---|---|---|---|
| **Q1** | 방위 중앙값 ≤ `10` **AND** 악화 ≤ 개선 | — | `13.801162888055714` · 16:14 | **FAIL**(중앙값) |
| **Q2** | recall > `0.5853658536585366` AND precision ≥ `0.70` | — | `0.2926829268292683` · `0.375` | **FAIL** |
| **Q3** | 앵커 A 비트 동일 | — | 전 항목 일치 | PASS |
| **Q3b** | 앵커 B 비트 동일 | — | 전 항목 일치 | PASS |
| **Q4** | 강등본 비트 동일 | — | 전 항목 일치 | PASS |
| **Q5** | recall ≤ `0.8780487804878049` | — | `0.2926829268292683` | PASS |
| **Q6** | 봉인 | — | green | PASS |
| **Q7** | md5 `d0af19b395edca366296cbd154f104f5` | — | 동일 | PASS |
| **Q8** | 신규 red 0 · tsc 0 | — | 신규 red 0 · tsc 0 | PASS |
| **Q9** | N1 대조 | 서술 | F4 이득 `0` < N1 이득 `0.5983810637389073` | **F4 = 원인 수리 아님** |
| **Q10** | best-of-N 상한 | 서술 | `11.100430381019102` / `11.007123357580006` | **T4 근거** |
| **Q11** | 원장 + W1~W4 | 서술 | 36행 · 개선16/악화14/폴백6 일치 | 완료 |

---

## 9. ★ §4-2 B 인계물 B1~B5 (T4 확정에 따라 작성)

### B1 — 기하로 불가능하다고 확정된 것

> **「seg 마스크 하단 콘투어를 직선 조각으로 분해해 앞변을 고르는 방식」은 골든 5프레임(마스크 36개)에서 방위 중앙값 `10`° 를 넘길 수 없다.**

증거 = **정제 후 best-of-N 중앙값**(계측 전용 오라클 상한):
- best-of-two `11.100430381019102`
- best-of-three `11.007123357580006`
- F4 정제 후 best-of-two `11.100430381019102`

**이것은 특정 규칙(R3)의 실패가 아니라 규칙 집합 전체의 상한이다.** 정답으로 최선을 골라 줘도 넘지 못한다. 육안 근거(§6-1 ①②)가 왜인지도 보여준다 — 콘투어 조각의 상당 부분이 **접지선이 아니라 실루엣 경계**이며, 실루엣 경계는 지면 방위 정보를 담지 않는다.

### B2 — 이미 시도해 실패한 것 (B 가 반복하지 말 것)

| 시도 | 방위 중앙값 | 회차 |
|---|---|---|
| chord (콘투어 좌우 끝 현) | `31.052940498468203` | 24 |
| kink + R3 (2선분 + 시선직교) | `13.801162888055714` | 26 |
| ⓒ 근측밴드 12px | `24.140026999999996` | 26 |
| 스팬 클램프 | `12.482965006828001` | 26 |
| best-of-two (상한) | `11.100430381019102` | 26·27 |
| **F4 꺾임점 고정 (27-A)** | **`13.801162888055714`(이득 0 — 방향 불변이라 원리적으로 무효)** | 27 |
| **N1 무작위 절사 (음성 대조군)** | **`13.202781824316807`(이득 `0.5983810637389073` — 수리보다 좋다)** | 27 |
| **C1 가림열 배제 (27-A)** | `13.801162888055714`(이득 `0`) | 27 |
| **best-of-three 3선분 (상한)** | `11.007123357580006` | 27 |
| R2 하단 선택 규칙 | `14.154963029704813` | 26·27 |
| R1 길이 선택 규칙 | `13.989642795383059` | 26·27 |

**추가 금지 항목**: `fitContactLine`(`contact.ts:343`) 은 **논리적 순환**이라 쓸 수 없다 — 깊이축 `b` 가 정해진 좌표계를 전제하는데, 그 좌표계를 정하려면 방위를 이미 알아야 한다. 26·27 회차 모두 봉인 유지. \|dy\| 중앙값도 `47.16350014200509` < `60` 으로 발동 조건 미충족.

### B3 — 살아 있는 것 = B 가 재사용할 파이프라인

`footprintFromContact`(`imageObservation.ts:111`) **이후 전 구간은 입력이 옳으면 옳다**. 강등본 앵커가 이를 증명한다:

> `--source degraded` → 산출 `42` · 회수면 `39` · **recall `0.9512195121951219`** · precision `0.9285714285714286` · 미회수 2면(검출미탐 1 · 기하결손 1) — **27-A 의 모든 변경 후에도 비트 동일**.

따라서 **B 는 방위(또는 앞변 2점)만 주면 되고, 게이트·배타성·채점·매칭은 손대지 않는다.**

### B4 — 인터페이스 1개

`proposeFromObservation`(`individualEngine.ts:163`)이 관측에서 읽는 필드는 **`o.footprintPx` 하나뿐**(`:164-165`).

> **B 모델의 산출 계약 = `PixelQuad` 1개.** 그 외 필드 불필요.

배선 지점도 하나다 — `individualEngine.ts:391-398` 의 소스 선택부에 B 소스를 추가하면 되고, `vpdSegSource` 는 건드리지 않는다.

### B5 — B 의 성공 판정선 (미리 못박음 · 사후 조정 금지)

| 항목 | 값 |
|---|---|
| 대상 | 같은 골든 5프레임 · 같은 frameHash `6006a034bfe2` / `ceaaed722663` / `3c0db12efe75` / `e33628e921c2` / `0cf4fda4d3aa` |
| 방위 | 중앙값 ≤ **`10`** |
| vpdseg recall(실검출) | > **`0.5853658536585366`** |
| precision | ≥ **`0.70`** |
| 강등본 앵커 | Q4 **비트 동일**(`42`/`39`/`0.9512195121951219`/`0.9285714285714286`) |
| 상한 정합 | recall ≤ `0.8780487804878049` (초과 시 오라클 누출 의심 — 즉시 중단) |
| 골든 무회귀 | rows md5 `d0af19b395edca366296cbd154f104f5` |

**B 도 미달이면 C 회군**(§6-4 K1 원문).

### B6 — B 에게 넘기는 추가 실측 (인계물 규정 밖이지만 유용)

1. **콘투어 오염의 정체는 「그림자」가 아니라 「실루엣 추종」이다** — C2 가 `dyKink` 부호 중앙값 `-38.51197117081026`, 음수 34/36 으로 그림자 가설(양수여야 함)을 **방향까지 반대로** 기각했다.
2. **`rejectOccluded` 는 이 오염을 원리적으로 못 잡는다** — 자기 마스크 안의 차체는 「다른 마스크」가 아니다(`contact.ts:140`).
3. **비-오라클 교차 대조**: seg(kink) ↔ LPD 판 방위 각 분포 `n=19 median=18.591661161570624`(min `1.0005801091022` max `35.622164594628096`). 두 독립 관측이 크게 갈린다 — 융합으로 메울 크기가 아니다.
4. **W1 이 준 음성 정보**: 오염률·깊이·해상도·면적·틸트 **어느 것도 악화군을 예측하지 못한다**. B 가 「어려운 케이스만 골라 다르게 처리」하는 설계를 세운다면, 그 분류 근거는 **이 5개 축에 없다**.

---

## 10. 변경 파일 목록 · 영향도

### 10-1. 신규

| 파일 | 내용 |
|---|---|
| `src/tools/contourRefine.ts` | `splitContour3`(C3·F3용 3선분 분해) · `dropOccludedCols`(F1) · `dropFarTail`(F2) · `colDepthM` · `dropRandomCols`+`seedOf`(Q9 N1). **전부 새 배열 반환(입력 불변)** · 봉인 토큰 부재 |
| `test/contourRefine.test.ts` | 6항목(불변성 · 무회귀 안전판 · 퇴화입력 · 오라클 봉인 · 배제토큰 봉인 · `vpdSegSource` 기본값 회귀) |

> **실사용 상태 명시**: `dropOccludedCols`·`dropFarTail` 는 **F1·F2 발동 조건 미충족으로 검출 경로에 배선되지 않았다**. 테스트로만 검증된 미배선 함수다. 「구현했으니 동작한다」로 읽히지 않게 여기 적는다.

### 10-2. 수정

| 파일 | 변경 | 검출 경로 영향 |
|---|---|---|
| `src/tools/groundErrProbe.ts` | `--autopsy`(단계 A 원장 + W1~W4) · `--diag`(C1~C6) · 단계 D 관문(kink2·N1·best-of-N) · `drawR27Overlays`(pre/post + 확대본) 추가. `MaskRec` 에 계측 필드 추가. **`drawEdgeOverlays`(r26 오버레이) 제거** — 내 변경으로 호출 지점이 사라져 고아가 됐다(CLAUDE.md §3). r26 의 kink 오버레이는 `r27_pre_*` 가 동일 내용으로 대체한다 | **없음** — 계측 전용 도구 |
| `src/tools/contactOrient.ts` | `KinkOpts.pinKink` 추가(**기본 `false`**) · `frontEdgeOf` 에 F4 분기 8줄 | **기본값에서 무변경** — Q3b 앵커 비트 동일로 확인 |
| `src/tools/imageObservation.ts` | `vpdSegSource` 의 `edge` 에 `'kink2'` 추가(**기본 `'chord'` 유지**) · 분기 2줄 | **기본값·`'kink'` 에서 무변경** — Q3·Q3b 앵커 비트 동일 |
| `src/tools/individualEngine.ts` | `--edge` 파싱에 `kink2` 1줄 | 인자 전달만 |

### 10-3. 불변 제약 준수

| 제약 | 증명 |
|---|---|
| 서비스 경로 무접촉 | `git status --porcelain -- src/ground src/rpc/services web` → **` M src/rpc/services/roiAuto.ts` 1건뿐이며 이는 27-B 것**(단계 0 스냅샷에 이미 있음). 내 변경 0 |
| 신규·수정은 `src/tools/`·`test/` 만 | 위 표 그대로 |
| `bottomContour` 재사용 유지 | 함수·호출 지점 무변경. 정제는 후처리로 설계했고, 실제로는 배선되지도 않았다 |
| 봉인 테스트 무손상 | `imageObservation.test.ts` 7개 · `contactOrient.test.ts` 6개 무변경 green |
| 오라클 검출경로 금지 | `best-of-N`·`dAng`·`truthQ`·`truthHead` 는 **`groundErrProbe` 계측 경로에만** 존재. `vpdSegSource` 로 흐르지 않는다 |
| Q4 강등본 비트 동일 | 전 변경 후 재실행 PASS |
| `toFixed` 판정 금지 | 판정·통계·원장 TSV 전부 원시 배정도. `toFixed` 는 SVG 좌표·스샷 헤더만 |
| 「살아났다」 금지 | Q9 대조군 필수 이행. **F4 를 원인 수리라고 쓰지 않았다** |
| 상한/실검출 구분 | §5 에서 표를 분리. best-of-N 은 매번 「계측 전용 오라클 상한」 명기 |
| 카메라 미사용 | `--fetch` 미실행 · `fillCache` 호출 0 |
| 정본·DB 쓰기 금지 | 쓰기는 `reports/overlay_r27a/` · `_workspace/` 하위만 |
| `git add -A`·커밋 금지 | 미실행. 27-B(`roiAuto.ts`·`roiAutoReplay.ts`·`vitest.config.ts`)·27-C(`frameArchive.ts`·`realGroundSplit.ts`) 무접촉 |

---

## 11. 미해결 · 설계자/리더 확인 요청

| # | 항목 | 상태 |
|---|---|---|
| **1** | **설계 §3 F4 의 잠재 결함**(§4-4) — C6 은 「스팬이 늘어나는가」만 쟀고 「그 스팬이 방위를 바꾸는가」를 묻지 않았다. 앞으로 수리 후보를 정의할 때 **「이 조작이 `footprintFromContact` 의 입력 중 무엇을 바꾸는가」**를 발동 조건에 포함할 것을 제안한다 | 설계자 확인 요청 |
| **2** | **C4b 지표 퇴화**(F-5) — `(chordLenM/4.7)/mpp` 는 차원상 판별력이 없다. ⓒ 는 **미분해로 남았다.** B 가 「병합 마스크」를 다룰 계획이면 이 측정을 재설계해야 한다 | 설계자 확인 요청 |
| **3** | **F0 1회는 소진하지 않았다** — 발동 조건 미충족 + 상한이 판정선 아래여서 쏘지 않았다(§4-3). B 회차로 이월 가능 | 리더 판단 |
| **4** | 지면 꺾임각 중앙값이 90° 가 아닌 이유 | **여전히 미분해**(26회차 승계) |
| **5** | `car.list` 좌표가 24회차와 동일한가 | **미측정**. Q4 비트 동일이 유일한 간접 확인(26회차 한계 승계) |
| **6** | 골든 d0 프레임만 사용(d1~d5 미사용) · 차량 치수 단일 가정 `4.7×1.85×1.45m` | 가정(23~27회차 승계) |
| **7** | 실카 σ · 실카 tilt 분포 | **미측정** — 27-B·27-C 소관 |

---

## 12. 산출물 경로

| 종류 | 경로 |
|---|---|
| 부검 원장(TSV 36행) | `_workspace/_r27a_ledger.tsv` |
| W1~W4 집계 | `_workspace/_r27a_W.txt` |
| 단계 0 git 스냅샷 | `_workspace/_r27a_gitstatus_before.txt` |
| Q7 골든 rows | `_workspace/_r27_final_rows.txt` (md5 `d0af19b395edca366296cbd154f104f5`) |
| 스샷 13장 | `reports/overlay_r27a/` |
| 신규 소스 | `src/tools/contourRefine.ts` |
| 신규 테스트 | `test/contourRefine.test.ts` |

### 재현 명령

```bash
npx tsx src/tools/groundErrProbe.ts v1 --cache reports/detcache_r24 --autopsy > _workspace/_r27a_ledger.tsv
npx tsx src/tools/groundErrProbe.ts v1 --cache reports/detcache_r24 --diag
npx tsx src/tools/groundErrProbe.ts v1 --cache reports/detcache_r24 --edge all --out reports/overlay_r27a
npx tsx src/tools/individualEngine.ts v1 --source vpdseg --edge kink2 --cache reports/detcache_r24
npx tsx src/tools/individualEngine.ts v1 --source degraded          # Q4
```

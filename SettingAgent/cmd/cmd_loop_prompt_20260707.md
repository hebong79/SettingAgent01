

[2026.07.08]
설계서를 쓰는게 아니라 중요한 키포인트만 쓴다.
정확한 목표와 중간중간의 요청사항(룰)을 생각하여 만들자.



----------------------------------------------------------------------------
 Goal:
 정밀수집을 시작하면 모든 카메라 프리셋마다 VPD와 LPD를 실행하고
 그 검출 데이터가 프리셋별로 DB에 저장되어야 한다.
 (현재는 총 3개 프리셋 중 1개 프리셋에만 VPD/LPD가 적용·저장됨 — 버그.)

 Loop:
 * 먼저 로그·DB로 사실을 확인한다 — 어느 프리셋이 실행/저장되고 어느 프리셋이
   누락되는지, 원인이 프리셋 순회(targets)인지·검출 호출인지·저장 단계인지 특정한다.
 * 원인을 분석해 설계한다.
 * 설계대로 코드를 수정한다.
 * 짧은 캡처(예: count 4)를 실행·최종화하고, DB에서 프리셋별
   검출/주차면 행 수를 조회해 3개 프리셋 모두 VPD/LPD가 실행·저장됐는지 확인한다.
 * 3개 프리셋 중 하나라도 누락되면 다시 분석·설계·수정한다.
 * 성공하면 내용을 정리해줘.

 Requirements:
 * 시작 시 3개 프리셋(1,2,3) 모두 VPD·LPD를 실행해야 한다.
 * VPD·LPD 검출 데이터가 프리셋별로 DB에 저장되어야 한다(프리셋 누락 0).
 * 특정 프리셋만 되고 나머지가 누락되면 실패로 간주한다.
 * 검증은 DB 조회(프리셋별 detection / parking_slots 행 수)로 확인한다.
 * 3회 이상 반복 실패하면 멈춘다.

----------------------------------------------------------------------------
 Goal:
 SQLite DB 테이블을 선택해서 볼 수 있는 페이지를 추가한다.

Loop:
 * 먼저 현재 코드(상단 탭 구조·라우트 등록·SqliteStore)를 분석해 설계한다.
 * 설계대로 구현/수정한다 — 상단 페이지 버튼 + DB 뷰 페이지 +
   테이블 목록/행 조회 라우트.
 * 서버를 켜고 페이지를 열어 테이블 콤보박스·테이블 뷰·검색이
   동작하는지 확인한다.
 * 실제 DB의 여러 테이블을 선택·검색해 결과(행/컬럼)가 맞는지 확인한다.
 * 동작 안 하거나 데이터가 안 맞으면 다시 분석·설계·수정한다.
 * 성공하면 내용을 정리해줘.

Requirements:
 * 상단(탭)에 페이지 버튼을 추가한다.
 * 페이지에서 콤보박스로 테이블을 선택할 수 있어야 한다.
 * 테이블 뷰에서 검색 기능이 존재해야 한다.
 * 완전 독립적으로 동작해야 한다 — DB만 사용(시뮬레이터·VPD·LPD·LLM 등
   외부 서비스에 의존하지 않고, 서버가 떠 있으면 언제든 동작).
 * 3회 이상 반복 실패하면 멈춘다.

----------------------------------------------------------------------------
 Goal:
 최종화 시 → 파일 바닥ROI 기준 주차면 리스트를 확정 → 각 주차면에 VPD/LPD/ROI/점유  (카메라·프리셋별)를 묶어 신규 DB 테이블(예: parking_slots)에 저장 + UI 리스트에 표시
 
 Loop:
 * 먼저 설계한다.
 * 설계된 코드를 구현/수정한다.
 * 테스트를 진행한다.
 * 실패하면 설계부터 다시 시작한다.
 * 성공하면 내용을 정리해줘.
 
 Requirements:
 * VPD와 LPD는 모두 화면에 표시 해야 한다.
 * VPD, LPD, ROI, 점유영역 모두 카메라기준 프리셋 모두 DB에 저장해야 한다.
 * 
 

----------------------------------------------------------------------------
 Goal:
 1. 정밀수집에서 시작버튼을 누르면 VPD, LPD, 바닥주차면 ROI 그리기를 해야 하고
 2. 바닥 LLM사용 체크박스가 꺼져있으면 LLM은 사용하지 않는다.
 3. 정밀수집페이지 하단에 주차면 목록에 등록 되어야 한다.
 4. 점유영역은 작은 원으로 표시한다.
 
 Loop:
 * 먼저 설계한다.
 * 설계된 코드를 구현/수정한다.
 * 테스트를 진행한다.
 * 실패하면 설계부터 다시 시작한다.
 * 성공하면 내용을 정리해줘.
 
 Requirements:
 * VPD와 LPD는 모두 화면에 표시 해야 한다.
 * 주차면등록은 파일에서 읽어온 주차면 바닥ROI를 기준으로 한다.
 * 바닥 LLM사용 체크박스가 꺼져있으면 LLM은 사용하지 않고 진행한다. 
 * 점유영역 판단은 로직으로 처리하고 최종화시 한번만 LLM이 맞았는지 판단한다.
 * 점유영역은 LPD 차량번호판 OBB박스 중심을 기준으로 하고 작은 원으로 표시한다.
 * 바닥 LLM사용 체크박스의 기본값은 꺼져 있는 상태여야 한다.
 



[2026.07.07]
----------------------------------------------------------------------------
 Goal:
 차량의 주차면 점유 판단을 위해 VPD를 실행해 bbox를 얻고, LPD를 실행해 OBB 값을 얻어 정보를 보관하고 화면에 출력한다. 
 
 Loop:
 * 현재 작업 코드를 분석하고 goal내용을 설계한다. 
 * 설계된 내용대로 코드를 작업 한다.
 * VPD로 차량을 검출하고 화면에 표시한다.
 * LPD로 차량 번호판을 검출하고 화면에 표시한다.
 * VPD로 차량은 찾았는데 LPD로 번호판이 검출이 안됐을때 VPD bbox 중심 앞쪽으로 약간 확대를 한다음
   다시 LPD로 번호판을 검출한다. 그래도 없으면 3번 더 zoom을 반복해서 LPD를 번호판을 검출한다.
 * zoom으로 확대되어 검출된 번호판 영역은 처음 LPD를 못찾았을때로 zoom을 원상복귀하여 
   LPD위치값을 역으로 계산한다.( 번호판의 줌심점만 비슷하면 됨, zoom은 fov 변환값임 )
 * 만약 화면에 vpd, lpd 영역이 출력되지 않으면 또는 영역데이터를 못얻었으면  다시 파일을  
   분석하고 코드설계, 코드를 수정한다. 
 * 성공하면 최종 결과를 설명해줘.
 
 Requirements:
 * VPD와 LPD는 모두 화면에 표시 해야 한다.
 * 10회 이상 반복 실패하면 멈춘다.   
 * 현재 해당 프리셋마다 체크한다. 
 * 현재 해당 프리셋이 정상일 때까지 Loop를 반복을 하고 다음 프리셋으로 이동한다.
 * 만약 현재 프리셋이 10회 반복실패하면 다음 프리셋으로 진행한다. 

----------------------------------------------------------------------------
 Goal:
 차량의 주차면 점유 판단을 위해 VPD를 실행해 bbox를 얻고, LPD를 실행해 OBB 값을 얻어 화면에 출력한다. 주차면위 차량 점유영역은 붉은색(반투명색) 작은원을 출력한다. ( 번호판 중심에 표시) 
 
 Loop:
 * 현재 작업 코드를 분석하고 goal내용을 설계한다. 
 * 설계된 내용대로 코드를 작업 한다.
 * VPD로 차량을 검출하고 화면에 표시한다.
 * LPD로 차량 번호판을 검출하고 화면에 표시한다.
 * VPD로 차량은 찾았는데 LPD로 번호판이 검출이 안됐을때 VPD bbox 중심 앞쪽으로 약간 확대를 한다음
   다시 LPD로 번호판을 검출한다. 그래도 없으면 3번 더 zoom을 반복해서 LPD를 번호판을 검출한다.
 * zoom으로 확대되어 검출된 번호판 영역은 처음 LPD를 못찾았을때로 zoom을 원상복귀하여 
   LPD위치값을 역으로 계산한다.( 번호판의 줌심점만 비슷하면 됨, zoom은 fov 변환값임 )
 * 만약 화면에 vpd, lpd 영역이 출력되지 않으면 또는 영역데이터를 못얻었으면  다시 파일을  
   분석하고 코드설계, 코드를 수정한다. 
 * 성공하면 최종 결과를 설명해줘.
 
 Requirements:
 * 점유역역 표시 작은원은 번호판 중심에 표시한다. 
 * 점유역역 표시 색상은 붉은색 (반투명) 이다. 
 * VPD와 LPD는 모두 화면에 표시 해야 한다.
 * 10회 이상 반복 실패하면 멈춘다.   
 * 현재 해당 프리셋마다 체크한다. 
 * 현재 해당 프리셋이 정상일 때까지 Loop를 반복을 하고 다음 프리셋으로 이동한다.
 * 만약 현재 프리셋이 10회 반복실패하면 다음 프리셋으로 진행한다. 




----------------------------------------------------------------------------
 Goal:
 LLM 토글 + 파일 ROI를 FloorRoiReviewer 실데이터로 연결하고 UI에 토글 추가해줘.
 
 Loop:
 * 현재 작업 코드를 분석해 설계한다. 
 * UI에 토글을 추가한다. 
 * 설계된 내용대로 코드를 수정한다.
 * UI 토글을 선택해 LLM 없이 사용을 테스트 한다.
 * 화면에 출력이 안되거나 실제 데이터 연결이 안돼 있으면 다시 파일을 분석하고 코드설계, 코드를 수정한다. 
 * 성공하면 최종 결과를 설명해줘.
 
 Requirements:
 * 파일로 부터 Json파일을 읽어서 실데이터와 연결한다.
 * LLM 사용모드는 연결만 하고 테스트는 안한다.
 * 3회 이상 반복 실패하면 멈춘다.   
 * SettingAgent\data\Place01\PtzCamRoi.json의 각 카메라의 프리셋 단위로 출력한다.
 * 현재 해당 프리셋마다 체크한다. 


----------------------------------------------------------------------------
 Goal:
 SettingAgent\data\Place01\PtzCamRoi.json을 참조하여 바닥 ROI를 그려줘.
 

 Loop:
 * SettingAgent\data\Place01\PtzCamRoi.json 열어 분석후 코드 설계를 한다.
 * 설계된 내용대로 코드를 수정한다.
 * 카메라 프리셋 별로 데이터 대로 화면에 그려졌는지 스샷을 찍어 확인한다.
 * 카메라 들의 모든 프리셋을 돌면서 확인한다.
 * 위치값이 많이 틀어져 있으면 다시 파일을 분석하고 코드설계, 코드를 수정한다. 
 * 성공하면 최종 결과를 설명해줘.
 
 Requirements:
 * SettingAgent\data\Place01\PtzCamRoi.json는 정밀수집 페이지에 왔을때 
   바로 로딩해서 데이터 정보를 가지고 있어야 한다.
 * SettingAgent\data\Place01\PtzCamRoi.json의 각 카메라의 프리셋 단위로 출력한다.
 * 현재 해당 프리셋만 출력한다.
 * 프리셋 마다 JOSN정보를 검수한다.
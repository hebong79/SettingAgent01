# MCP 자동 바닥 ROI 생성 — 1~14회차 전체 기록 · 다음 액션

- 작성: 2026-07-29 14:36:56 / 리더(오케스트레이터)
- 실행 모드: **goal/loop B 모드** (parkagent-dev 하네스)
- 목표: 2카메라 전 프리셋(23면)에서 **주차면 개별 IoU ≥ 0.98** → **14회차 시점 마스터 조정: ≥ 0.95**
- 조건: **이미지에서 사각형 4점을 직접 검출**. 수동 ROI 는 채점에만(hold-out). 실카에서도 성립
- 상태: **목표 미달 · 진행 중.** 정본·DB 무접촉, `roi.auto.apply` 0회 실행

---

## 0. 한 눈에 보기

| 회차 | 한 줄 | 평균 IoU | 판정 |
|---|---|---|---|
| 1 | 도색선 검출 + 호모그래피 최초 구현 | 0.09116 | 0/18 |
| 2 | 도색지지 점수 도입. **소실점 붕괴 원인 규명** | 0.15560 | 0/19 |
| 3 | ★ **f 를 제원에서 주입**(경로 교체) | 0.57942 | `2:1` 0.033→0.931 |
| 4 | 높이 자가보정(4.951/4.960m) | 0.58383 | — |
| 5 | `aimCenterWeight` 도입. `1:2` 0 탈출 | 0.72496 | — |
| 6 | LOPO 로 `aimCenterWeight` **과적합 의심** 판정 | 0.72496 | 수치 0, **최고 가치** |
| 7 | ★ **잔차 분해** — 결손 전체가 근변선 단일 성분 | 0.72496 | 원인 특정 |
| 8 | 2-DOF 재적합 → **최초 ≥0.98 면 3개** | 0.74229 | 3/21 |
| 9 | 24관측 LOPO → `aimCenterWeight` **「검증됨」** | 0.74229 | `2:2` 「임계」 재분류 |
| 10 | 수렴반복 가설 반증. 후보 **7건 제거** | 0.74229 | 소거법 |
| 11 | 스트라이프 규약 반증. **중심선 비직선** 단서 | 0.74229 | 소거 9건 |
| 12 | ★ **`2:2` 0 → 0.94410**(다시점 합의) | 0.75668 | 소거 10건 |
| 13 | JPEG 압축 가설 반증. **잡음 바닥** 제기 | — | 소거 12건 |
| 14 | ★★ **프레임 종속 발견 — 위 수치 대부분 해석 불가** | **무효** | 타당성 결론 |

> **★ 14회차 경고를 적용하지 않고 위 표를 읽지 말 것.** §15 참조.

---

## 1~2회차 — 착수와 첫 벽

**1회차.** 노면 도색선(LSD/Hough) → 선분 → 교점으로 근변 코너를 잡고, 바닥 평면 가정 위에서 호모그래피로 격자를 투영하는 구조를 구현. 실프레임 **0/18 면**, 평균 0.09116. 다만 **합성 장면(정답 해석적, 가림 포함)에서는 전 면 0.9879~0.9959**, f 회수 오차 0.86%, 지상고 0.18% — **기하 자체는 맞다**는 근거를 확보.

**2회차.** 재투영 도색지지 점수(P1)를 도입. 정답 행 단독 투입 시 가설 97개가 **전부 사망**했고 94개가 `D5_VP_DEGENERATE`, 그중 **79개가 `focalFromVPs` 부호 실패**.

> **원인 규명:** 참 분리선은 **645px** 인데 차량이 즉시 가려 검출 스텁이 평균 **24px**. 24px 에서 서브픽셀 잡음은 **±2.4° 각도 불확실성**을 만들고 그것이 소실점을 수천 px 날린다. **코너 위치는 멀쩡한데(0.1~15px) 방향이 사라진다.**

---

## 3회차 — ★ 전환점: f 를 추정하지 말고 주입하라

리더가 카메라 제원에서 f 를 계산해 실측 추정치와 대조:

| 프리셋 | f 예측(제원) | f 실측(추정) | 오차 | 당시 IoU |
|---|---|---|---|---|
| **1:1** | 2932.8 | 2913.4 | **−0.66%** | **0.489** |
| 1:2 | 2736.2 | 1234 | −54.9% | 0.000 |
| 2:1 | **5651.5** | 923 | **−83.7%** | 0.000 |
| 2:2 | 5651.5 | 1593.8 | −71.8% | 0.000 |

**f 가 맞은 유일한 프리셋이 IoU 가 0 이 아닌 유일한 프리셋이었다.**

그리고 f 는 이미 알려져 있었다 — `PtzCamRoi.json` 이 **프리셋마다 자체 `fov`·`eulerAngles`** 를 담고, `camera.position[1]`(설치고)이 있다. 이 모델로 수동 코너를 지면 역투영해 실치수를 재니:

```
전 5개 프리셋:  폭 2.525m(+1.0%)  깊이 5.051m(+1.0%)  비율 정확히 2.000
```

→ `focalFromVPs` 경로 **폐기**, `CameraIntrinsicsProvider` 로 주입. `2:1` 0.033 → **0.931**.

**R1(hold-out) 위반이 아닌 근거:** `fov`←실카는 `lens_calibration.json` 의 `zoomHfov` / `tilt`←PTZ 피드백 / `설치고`←줄자 실측. 전부 **카메라 사실**이지 주차면 정답이 아니다.

---

## 4회차 — 높이 자가보정

유니티 `preset.list` 로 **참 주차면 치수가 정확히 2.5×5.0m** 임을 확인 → +1.0% 편차의 원인은 슬롯이 아니라 **높이**. 검출 근변코너 간격을 규격 2.5m 에 맞춰 높이를 역산:

```
1:1  5.000m → 4.951m (계수 0.99018)
2:1  5.000m → 4.960m (계수 0.99194)
```

**참 설치고 4.950m 를 3가지 독립 방법이 일치 확인**(설계자 이미지추정 / 리더 역투영 역산 / 구현자 자가보정). 명목값 5.0m 가 5cm 틀렸다.

> ⚠ **14회차 후속:** 이 자가보정이 **미세 프레임 차이를 IoU 0.21 로 증폭**하는 경로이기도 하다. §16 참조.

---

## 5~6회차 — `aimCenterWeight` 와 과적합 판정

**5회차.** 「틀린 승자」의 정체가 밝혀졌다 — 가짜 도색선이 아니라 **이 프리셋이 주석하지 않은 다른 진짜 주차 행**이었다. 도색 증거가 원리적으로 둘을 가를 수 없는 이유가 이것. 해법으로 **`aimCenterWeight`**(프리셋 PTZ 가 대상 행을 겨눴으므로 그 행이 화면 중앙 부근에 온다 — 카메라 사실) 도입. `1:2` 0 탈출.

**6회차.** 수치 향상 0. 그러나 LOPO 5폴드로 **「과적합 의심」** 판정:

| 제외 프리셋 | 선택 aimW | 제외본 결과 | 기준 |
|---|---|---|---|
| 1:2 | 0 | 0.00000 | 효과 전이 안 됨 |
| **2:1** | **3.0** | **0.10307** | 0.93117 (**−0.888 붕괴**) |

**`1:2` 만이 1.6 을 지지하고 `2:1` 만이 3.0 을 배제한다** — 안전구간 양 끝이 각각 관측 1건. **5프레임으로는 정당화 불가.**

---

## 7회차 — ★ 잔차 분해로 원인 특정

`roiAutoResidual.ts` 로 자동·수동 quad 를 **같은 지면모델**로 역투영해 미터 비교:

| 프리셋 | 근변 오프셋 | 폭 오차 | 깊이 오차 | 전단 |
|---|---|---|---|---|
| 1:1 | **+0.0800m** | −0.02% | −0.02% | −0.00° |
| 2:1 | **+0.0563m** | −0.20% | −0.20% | −0.00° |

**폭·깊이·전단이 전부 0.2% 안. 13면 결손 전체가 근변선 오프셋 하나.** 게다가 행을 따라 단조 변화하고 전단은 0 → **평행이동이 아니라 회전 0.16~0.31°**. 원인은 **부분 가림된 지지구간 위 TLS 적합**으로 지레팔이 짧아 각도 편향.

**정본 결함도 함께 발견:** `1:1`·`2:1` 수동은 정확(2.500/5.001, 2.505/5.010)한데 **`1:2` 수동이 5~12% 과대**, **`1:3` 은 축이 정확히 뒤바뀜**.

---

## 8회차 — 최초 `≥0.98`

전 구간(61표본, 기존 15) 2-DOF 재적합 → 오프셋 1:1 0.0800→**0.0398m**, 2:1 0.0563→**0.0336m**. **이 프로젝트 최초의 ≥0.98 면 3개**(1:1 s1=0.9854, s2=0.9824 · 2:1 s19=0.9843).

**잔여 문제:** 재적합이 회전을 **지나쳐 보정**(부호 반전). 평균은 절반이 됐으나 기울기가 중심을 넘어가 행의 한쪽 끝만 0.98 을 넘는다.

---

## 9회차 — `aimCenterWeight` 「검증됨」

PTZ 지터로 **24관측**(5프리셋×6지터) 확보 후 LOPO 재실행 → `{0, 1.6}` **4폴드 전부 통과**. 전체 평균 0.42610→**0.73823(+73%)**. **상한 확정: 3.0 기각, 재튜닝 시 2.2 캡.**

**구현자가 자기 정답 유도 버그를 발견:** tilt 지터가 0.2420 으로 깨진 것을 보고 ①`X` 가 기저 카메라 프레임에 남음 ②pan 회전축이 카메라 y축이 아니라 **월드 수직** — 두 오류를 찾아 0.8737 로 복구. **안 잡았으면 LOPO 결론 전체가 틀린 정답 위에 세워졌다.**

**`2:2` 재분류:** 같은 행을 1.5° 다르게 보면 0.9440/0.9521 → 검출·기하 실패가 아니라 **기저 PTZ 가 선별 임계에 걸린 불운한 배치**. 「미해결」→「임계」.

---

## 10~11회차 — 소거법

**10회차.** 재적합은 **1~2회에 이미 수렴**(2·5회차 바이트 동일). 8회차 오버슈트는 덜 돈 게 아니라 **수렴한 고정점 자체가 편향**. 감쇠 0.5 도 같은 고정점에 더 느리게 갈 뿐이고 IoU 하락. 표본 밀도 가설도 사망(61표본 중 59개 생존 = 불균형 없음). **후보 7건 제거.**

**11회차.** 스트라이프 규약 반증 — `r = (수동−중심)/반폭` 이 **−0.08~−0.11**(모서리면 ±1). **수동·자동 둘 다 중심선**을 잡는다. 대신 새 단서: **도색 중심선이 이미지에서 직선이 아니다**(국소 중심이 적합직선 대비 1.1~1.3px 드리프트, 수동은 그 곡선을 따라간다. `tM−tC` 가 두 프리셋에서 +0.501/+0.498px 일치). **소거 9건.**

---

## 12~13회차 — `2:2` 해결과 잡음 제기

**12회차.** 다시점 합의로 **`2:2` 0 → 0.94410**. 잘 되는 프리셋은 6시점 만장일치라 기저가 대표로 남아 **비트 동일**(구조적 무회귀). 리더의 인공물 가설(폭 비례 창) 반증. **소거 10건.**

**13회차.** JPEG 압축 가설 반증(PNG 무손실에서도 드리프트 잔존, 방향 엇갈림). **잡음 바닥 제기** — 동일 PTZ 재캡처에서 드리프트 1.24→1.49px. **소거 12건.**

> ⚠ 이 잡음 수치는 14회차에 **JPEG/PNG 혼입**으로 판정 보류됨.

---

## 14회차 — ★★ 결론은 수치가 아니라 타당성

### 14-1. 프레임 종속

**같은 PTZ·같은 코드인데 잡힌 프레임에 따라 `≥0.95` 통과면수가 ±6면 변한다.**

```
1:1  프레임 65182cdf → 평균 0.85541 (≥0.95  0/7)
     프레임 6006a034 → 평균 0.97474 (≥0.95  7/7)
2:1  프레임 80cdf49c → 평균 0.96614 (≥0.95  4/6)
     프레임 e33628e9 → 평균 0.85402 (≥0.95  1/6)
```

각 프레임은 재현 100% → **잡음이 아니라 결정론적 종속.**

> **★ 7~13회차 회차간 비교(0.72496 → 0.74229 → 0.75668)는 무효다.** 알고리즘 개선인지 프레임 운인지 구분되지 않는다.
> **프레임 해시 없는 IoU 수치는 해석 불가로 간주하라.**

### 14-2. 서비스 붕괴의 진짜 원인 — 카메라 전환

`config/tools.config.json` 이 **12:46 에 `selectedCameraId: "real-camera-2"` 로 바뀌어**, 이후 `roi.auto.score` 가 **실카(192.168.0.154) 프레임을 시뮬 기준 수동 정본으로 채점**하고 있었다. 로그 증거 `setting_20260729_132915.log` 의 `jpeg.cgi` 호출. 서비스 실행 3건 폐기.

- **13021 은 무죄** — PID 11544 로 살아 있으나 로그가 00:48 이후 0바이트(무활동)
- **교훈:** 전후로 **같은 명령이 전혀 다른 대상을 찍는데 응답 형식이 동일**했다. 로그 없이는 구분 불가

### 14-3. 유효한 성과

- **tilt 미보정이 실제 결함** — `roiAuto.ts:detectOne` 이 디더 프레임에 기저 프리셋 tilt 로 지면모델을 세웠다(디더는 tilt ±0.8°, tilt 17° 에서 상대 4.7%). 고정프레임 통제비교에서 **`2:2` 0 → 0.94182**, ≥0.95 **11면 → 13면**. 득표는 양쪽 다 3/2/1 인데 **승리 군집이 달랐다**
- **골든 프레임 세트 확립** — `test/fixtures/roiAutoGolden/` 30장 + `manifest.json`(10.0MB). **앞으로 알고리즘 비교는 이 위에서만**
- **`frameHash` 배선** — `roi.auto.detect`/`score` 응답 + 합의 issue 의 시점별 6개 해시
- **P4 봉인** — `bayGrid.ts` 를 `DETECT_MODULES` 에 추가(green, 실제 오염 없음) + **메타 테스트**(신규 검출 모듈이 봉인·예외 어디에도 없으면 실패)
- **P2 좌표 융합은 3기준 전부 실패**(s14 0.8701 / `2:2` 2면 미달 / `1:1` −0.031·`2:1` +0.072, 통과면 13→10) → 서비스 미배선, `src/tools/roiAutoFuse.ts` 에 진단용 보존

---

## 15. ★ 확립된 사실 (재조사 금지)

| # | 사실 |
|---|---|
| F1 | 노면 도색선이 5개 프리셋 전부에 존재(백색 1.99~5.67%) |
| F2 | **시뮬이 초록 주차면 박스를 렌더한다 → 반드시 `roi.show2d {visible:false}`**. `preset.setBoxVisible` 은 무효. 끄면 `1:2` 백색 2.47→4.49%(박스가 도색선을 덮고 있었다) |
| F3 | 참 전방선은 이미 검출·상위랭크(1:1 rank0 0.69~2.26px · 2:1 rank0 0.88~2.35px · 2:2 rank1 **0.07px**) |
| F4 | **f 를 소실점에서 추정하면 안 된다**(스텁 24px → ±2.4° → VP 수천px 이탈) |
| F5 | **f 는 카메라 제원에서 주입**(프리셋별 `fov`·`eulerAngles` + 설치고). 실카는 `lens_calibration.json` |
| F6 | **카메라 설치고 = 4.950m**(명목 5.0m 아님). 3중 독립 확인 |
| F7 | **참 주차면 치수 = 2.5×5.0m**(유니티 `preset.list`) |
| F8 | 정점 배정은 정답지 없이 판별된다(V7 성공) |
| F9 | 기하 자체는 맞다(합성 장면 전 면 0.9879~0.9959) |
| F10 | PTZ 재현오차 0.000(**시뮬 한정** — 실카는 수~수십px) |
| F11 | **다중 행 장면이 표준**. 0.977 받는 `1:1` 프레임에도 두 번째 도색 행이 있다 → `aimCenterWeight` 는 필수 구성요소 |
| F12 | **`aimCenterWeight` = 1.6, 상한 2.2**(3.0 기각). 24관측 LOPO 「검증됨」— **시뮬 한정** |
| F13 | **프레임 해시 없는 IoU 는 해석 불가**(14회차) |
| F14 | DB 쓰기는 **`slot.roi.sync` 만**. `slot.roi.load`·`replaceSlotSetup` 은 검출·점유·센터링 전량 소실(실측 23→0) |

---

## 16. ★★ 반증 목록 20건 — 다시 시도하지 마라 (전부 실측으로 죽음)

> **이 절이 이 문서에서 가장 실용적인 부분이다.** 14라운드를 돌며 그럴듯하지만 틀린 가설이 20개 나왔고,
> **그중 5건(#1 #5 #12 #13 #18)은 리더가 승인했다가 구현자가 실측으로 뒤집은 것**이다.
> 재개 세션이 이 목록을 건너뛰면 죽은 가설을 다시 파느라 몇 시간을 잃는다.

| # | 가설 | 반증 회차 | 제기자 | 결과 |
|---|---|---|---|---|
| 1 | 지상고 편차를 선택 **1순위 정렬키**로 | 1 | 구현자 | 지상고는 전 프리셋 4% 내로 맞았는데 **1:1 IoU 0.6004→0.0049 붕괴**. 필요조건이지 충분조건 아님 |
| 2 | `focalFromVPs` 로 **f 추정** | 2 | 설계 | 24px 스텁에서 **79/97 가설 부호 실패**. 경로 폐기 → 제원 주입으로 교체 |
| 3 | 더 **긴 분리선 스텁** 찾기 | 2 | — | 이미지에 **존재하지 않는다**(차량 가림) |
| 4 | 격자를 **카메라 쪽으로 확장** | 3 | 구현자 | 전 프리셋 **무변화** |
| 5 | **`phaseFitM` 가중치 상향** | 4 | 구현자→리더 승인 | 어떤 값에서도 1:2·2:2 구제 실패, 2:1 0.931→0.863 악화. `1:2` 는 **반대 방향** 판별. *구현자가 "내가 준 근거가 반쪽이었다"고 자진 신고* |
| 6 | 슬롯 치수를 **2.525** 로 | 4 | — | 참 치수는 2.5(F7). 원인은 **높이**(F6) |
| 7 | **도색지지 단독** 적용 | 3 | 구현자 | recall 손실로 오히려 악화(0.09116→0.08300). 격자확장과 **함께**만 유효 |
| 8 | **`farWeight` 상향** | 5 | 구현자 | **정답 행의 원변 도색지지가 오히려 낮다**(2:2 정답 0.127 vs 승자 0.262 **역전**). 차량이 원변을 가려 도색이 원리적으로 약함. 뒤집으려면 8.4 필요 |
| 9 | **거리 기반 하드 게이트** | 6 | 리더 | "31m vs 10m = 3.1배"는 y좌표 **눈대중**이었다. `medianDepthM` 실측 시 **2.0배 게이트로도 미분리** |
| 10 | 커버리지 페널티 **완화·제거** | 6 | 리더 | `2:2` 결손 베이는 **끝단**이라 내부/끝단 규칙으로 못 구함. 제거 시 `2:1` **0.109 붕괴** |
| 11 | `maxHeightCorrection` **15→5%** | 6 | 구현자 | `1:2` 인자 1.04365 로 **이미 5% 안**. 무변화. *구현자가 자기 산술 착오로 신고* |
| 12 | **재적합 반복 증가** | 10 | 리더 | **1~2회에 이미 수렴**(2·5회차 **바이트 동일**). 8회차 오버슈트는 덜 돈 게 아니라 **수렴한 고정점 자체의 편향** |
| 13 | **감쇠계수 0.5** | 10 | 리더 | 같은 고정점에 더 느리게 갈 뿐. IoU 하락(1:1 0.97667→0.97559, 2:1 통과면 1→0) |
| 14 | **표본 균등 가중** | 10 | 구현자 | 61표본 중 **59개 생존** = 밀도 불균형이 애초에 없었다. 전제 자체가 거짓 |
| 15 | **스트라이프 모서리 규약** 보정 | 11 | 구현자 | `r=(수동−중심)/반폭` 이 **−0.08~−0.11**(모서리면 ±1). **수동·자동 양쪽 다 중심선** |
| 16 | **폭 비례 창**(추정기 인공물) | 12 | 리더 | 드리프트 불변(1.17 vs 1.11px). 파이프라인 **자체 추정기가 오히려 최대**(1.24px) |
| 17 | **"도색이 3D 에서 비직선"** | 12 | 리더가 제거 | 유니티 `preset.get`: `faceRot=0`·`groupRot=0`·단일 `offsetPos`·균일 피치 = **3D 에선 완벽한 직선** |
| 18 | **JPEG 압축 인공물** | 13 | 리더 | PNG **무손실에서도 드리프트 잔존**, 1:1 은 오히려 증가(1.24→1.49px), 방향도 엇갈림 |
| 19 | **P2 좌표 융합**(중앙값) | 14 | 마스터 지시 | 성공기준 **3개 전부 실패**(s14 0.8701 / 2:2 2면 미달 / 1:1 −0.031·2:1 +0.072). 통과면 **13→10** |
| 20 | **캡처 잡음 = 0** | 14 | 구현자 자진 정정 | 씬이 정지한 **짧은 창 안에서만** 참. 일반 명제로는 거짓 |

### 16-1. 목록에서 배울 것 (메타)

- **#1 #12 #13** — "물리적으로 그럴듯한 양"(지상고·수렴·감쇠)이 **선택 기준으로는 틀린다**. 격자의 **내부 일관성**만 보는 구속은 격자를 통째로 옮겨도 만족된다
- **#8 #15 #17 #18** — **가림이 만드는 증거 부재**를 "검출 실패"로 오인하면 안 된다. 원변 도색은 원리적으로 없다
- **#9 #11 #20** — **눈대중·산술착오·조건부 참**을 일반 명제로 승격하면 다음 라운드가 통째로 오염된다. 세 건 모두 **제기자가 스스로 정정**해서 막았다
- **#5 #16 #18** — 리더가 승인한 가설도 실측 앞에서는 예외가 아니다. **근거가 합리적인 것과 사실인 것은 다르다**

---

## 17. 미해결 (U 목록)

| # | 항목 |
|---|---|
| **U1** | **10면 정체 — 원인 미규명.** 후보 12건 소거. 단서: 도색 중심선이 이미지에서 비직선(1.1~1.3px). 마스터 지시로 **원인 탐색 중단**(0.95 목표에서는 관문이 아님) |
| **U2** | DB 고아 행 `slot_id 24`(cam2:p1#7) — 파일에선 제거됐으나 **삭제 RPC 가 없다** |
| **U3** | `idx8` 앵커 미교정 — 앵커 186px/이웃 177px 로 **둘 다 도색선에 안 붙음**. `1:2` 검출 개선 후 재판정 |
| **U5** | **실카(RTSP) 파이프라인 검증 0건.** 전 수치가 시뮬 |
| **U15** | ★ **실카 f: 펌웨어 값을 믿으면 안 된다.** `focalTrue` vs `focalFirmware` 최대 **7.7%** 어긋남(zoom 3000: 2403.53 vs 2588.87). 실카 provider 는 `focalTrue` 를 zoom 보간해 쓸 것 |
| **U16** | **곡면율은 지금 쓸 수 없다.** `k1`/`k2` 계수 부재, PTZ 시차 미분리로 미채택(잔차 31.7~131.2px). **순서: ①`focalTrue` 보간 → ②직진성 재측정 → ③그때도 휘면 재캘리브레이션** |
| **U17** | **씬 변화 미확정** — 3분에 9종 프레임 vs 4분 바이트 동일. 실카 정찰 중 동시 간섭 가능성이 남아 판정 보류 |

---

## 18. 실카 정찰 결과 (14회차, 리더 직접 수행 — 이 프로젝트 최초 실카 실측)

`GET /viewer/api/snapshot?source=real-camera-2&cam=1&preset=1` → 1920×1080 JPEG, 0.48초

| 항목 | 실카(154) | 시뮬 |
|---|---|---|
| 백색 픽셀 | **6.79%** | 1.99~5.67% |
| 초록 합성단서 | 0.000% | (차단 후 0) |
| 평균 밝기 | 102.6 | — |

**① 노면 도색선 실재 확인 — 접근 전체의 전제가 실카에서 성립한다.** (가장 큰 소득)
**② 백색의 상당분이 흰 차량이다** — 20대 이상 중 절반 가까이 백색·은색. 시뮬은 차량이 어두워 백색=도색선이었다 → **`paintMask` 가 실카에서 차체로 오염된다**
**③ 주차 행이 4~5줄** 동시에 보인다(시뮬 2줄) → `aimCenterWeight` 가 실카에서 더 필수적
**④ 가림이 더 심하다** — 슬롯 경계 수직선이 거의 안 보인다
**⑤ 장면의 절반이 주차장이 아니다** — 건물·나무·전신주·수로. 나무 그림자와 외벽 라인이 유사 직선 증거를 만든다
**⑥ ★ PTZ 프리셋이 정의돼 있지 않다**(`"현재 위치"` 1개) → **프리셋별 지면모델 주입 구조가 그대로는 안 돌아간다**
**⑦ `real-camera-2` 는 자체 캘리브 없음**(`model: cam-001`). `focalTrue` 보유는 `real-camera-1`(153)뿐

---

## 19. ▶ 다음 액션

### 19-1. 마스터 결정 대기 (착수 전 필요)

| # | 항목 | 상세 |
|---|---|---|
| **A1** | **카메라 소스 복귀** | `selectedCameraId` 가 아직 `real-camera-2`. 그동안 서비스 라이브 채점 전부 무의미. 시뮬 복귀 여부 |
| **A2** | **`1:2` 채점 제외 — 구현자 반대** | 권고 근거 "최대 IoU 0.827~0.900" 이 **지상고 자가보정이 격자 배율을 자유롭게 만들어 구속력이 없다**. 실측 `s8=0.9073 s9=0.9553 s10=0.9698 s11=0.9825` → ≥0.95 **3/4 로 현재 최고 프리셋**. 제외 시 통과면 3개 소멸. **회신 전까지 현행 유지·양쪽 병기** |
| **A3** | **P2 융합 미배선(문언 이탈)** | 지시는 "옵션 플래그로 강등"이었으나 3기준 전부 실패(13→10면)라 진단 도구로만 보존. 플래그로라도 넣을지 |
| **A4** | **골든 세트 10.0MB** | `test/fixtures/` 라 `.gitignore` 미적용. 커밋 대상 여부 |
| **A5** | **정본 결함 3건**(이월) | ①`1:2` 수동 5~12% 과대 ②`1:3` 축 뒤바뀜 ③`idx8` 교정(독립 두 측정이 같은 결함 지목) |

### 19-2. 15회차 최우선 — 지상고 자가보정이 「증폭기」인가

**가장 값진 미탐색 항목이며 반증목록에 없다.**

```
MAD 0.068 짜리 프레임 차이 → 칸간격 −4.9% 이동 → IoU 0.21 흔들림
CALIB=0 :  1:1  0.855 → 0.932
           2:1  k1/k3 격차 0.115 → 0.0002 로 소멸
```

4회차에 리더가 지시한 높이 자가보정(F6 근거)이 **미세 프레임 차이를 증폭하는 경로**이기도 하다. **골든 세트가 생겨 이제 통제 비교가 가능하다 — 14회차 이전에는 이 비교 자체가 성립하지 않았다.**

검증 방법: 골든 30장 위에서 `CALIB` on/off A/B. 끄면 좋아지는지, 켜야 하는 경우는 어떤 조건인지.

### 19-3. 그다음 (순서 있음)

1. **골든 세트 위에서 0.95 기준 면별 표 확정** — 프레임 해시 명시. 알고리즘 소관 잔여 면수를 여기서 처음으로 정확히 셀 수 있다
2. **U17 씬 변화 판정** — 실카 정찰 간섭이 없는 상태에서 probe 재실행
3. **실카 전환 준비**(U15·U16 순서 준수) — ①`real-camera-1`(153) 정찰(zoomHfov 13점 + `focalTrue` 보유) ②PTZ 프리셋 정의 ③`focalTrue` 보간 provider ④그 뒤 파이프라인
4. **CLAUDE.md 5대 규칙 마감 단계** — **qa-tester(검증)·documenter(문서화)가 아직 미실행**. 루프가 성공 판정에 도달하지 않아 마감으로 못 갔다

---

## 20. 산출물 위치

| 종류 | 경로 |
|---|---|
| **재개용 체크포인트** | `.claude/checkpoints/CHECKPOINT.md` |
| **지식 정본(확립사실·미해결·반증)** | `_workspace/00_leader_handoff.md` |
| 리더 판단 기록 | `_workspace/00_leader_{context,decisions,loop1_analysis,loop2_analysis}.md` |
| 설계 | `_workspace/01_architect_plan.md` |
| 라운드별 구현 보고 | `_workspace/02{,b~n}_developer_changes*.md` |
| 선행 설계서 | `docs/20260729_010651_MCP자동_바닥ROI_생성_설계.md` |
| 신규 소스 | `src/ground/{floorPaint,bayGeometry,bayGrid,roiAutoScore,cameraIntrinsics,placeMetaIntrinsics}.ts` · `src/rpc/services/roiAuto.ts` |
| 진단 도구 | `src/tools/{roiAutoBench,roiAutoOverlay,roiAutoDiag,anchorCheck,roiAutoRowWhy,roiAutoResidual,roiAutoFuse}.ts` |
| **골든 프레임 세트** | `test/fixtures/roiAutoGolden/` (30장 + manifest, 10.0MB) |
| 세션 메모 | `메모/memo.md` |

---

## 21. 안전 상태 (14회차 종료 시점)

```
정본 PtzCamRoi.json : 마스터 지시분(슬롯24 제거 · idx1 앵커 교정) 외 무변경
DB slot_setup       : 검출·점유·센터링 보존 (slot.roi.sync 비파괴 경로만 사용)
회귀                : tsc --noEmit exit 0 · vitest 281파일 3553테스트 전량 green
roi.auto.apply      : 0회 실행
서버 재시작          : 0회 / config 변경 : 리더·구현자 0건
```

**실카 검증은 파이프라인 기준 여전히 0건이다. 시뮬 수치로 실카를 대변하지 않는다(R10).**

# 15회차 재개 지시서 — 테스트 후 이어서 하기 위한 문서

- 작성: 2026-07-29 17:55 / 리더(현자 라)
- 용도: **마스터의 테스트가 끝난 뒤 이 파일 하나만 읽고 바로 이어갈 수 있게** 하는 재개 문서
- 상태: **15회차 완료.** 서비스·정본·DB 무변경 · `roi.auto.apply` 0회 · `tsc` 0 · vitest 281파일 3553 green

> 재개 시 읽는 순서: **이 문서 → `docs/20260729_171526_...15회차_골든v2와_증폭기통제.md`(정본) → `_workspace/00_leader_handoff.md`(확립사실·반증목록 20건)**

---

## 0. 지금 당장 확인하고 싶다면 (마스터용)

### (a) 눈으로 보기 — 오버레이 PNG
```
SettingAgent/reports/overlay_r15/v1/overlay_{1_1,1_2,1_3,2_1,2_2}.png
SettingAgent/reports/overlay_r15/v2/overlay_{1_1,1_2,1_3,2_1,2_2}.png
```
**초록 = 수동 정본(정답) · 빨강 점선 = 자동 검출 · 노랑 = 선택된 전방선 · 주황 ○ = 근변 코너.**
`v1` 과 `v2` 의 **같은 파일명을 나란히** 열면 이번 라운드의 핵심이 바로 보인다 — **같은 PTZ·같은 코드인데 프레임만 다르다.**

직접 재생성:
```bash
cd SettingAgent
npx tsx src/tools/roiAutoOverlay.ts test/fixtures/roiAutoGolden    reports/overlay_r15/v1 data/Place01/PtzCamRoi.json 0
npx tsx src/tools/roiAutoOverlay.ts test/fixtures/roiAutoGolden_v2 reports/overlay_r15/v2 data/Place01/PtzCamRoi.json 0
# 마지막 인자 0 = 디더 시점 인덱스(0~5). 기저 시점이 0
```

### (b) 수치로 보기 — 라이브 채점 1회 (서버 13020 구동 중)
```bash
curl -s -X POST http://localhost:13020/rpc -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","id":1,"method":"roi.auto.score","params":{"camId":1,"presetIdx":1}}'
```
정상 판정: `meanIoU` 가 **0.8~1.0 대**. `0.0x` 로 붕괴하면 카메라 소스가 시뮬이 아니다.
2026-07-29 17:41 실측 = **0.85540**(프레임 `77e5bb90f3be`). **정본·DB 무접촉 읽기 전용이다.**

### (c) 고정 세트로 전체 비교 — A/A0/B/C
```bash
cd SettingAgent
npx tsx src/tools/roiAutoFuse.ts                     # 인자 없음 = v1 · A(현행). 14회차와 동일 출력
```
v1/v2 × A/A0/B/C 전체 표는 `_workspace/15_scoreboard.md` · `15_fuse_v1.log` · `15_fuse_v2.log` 에 이미 있다.

> ⚠ **`roi.auto.apply` 는 실행하지 마라.** 정본을 쓴다. 15회차까지 0회 실행 상태를 유지 중이다.

---

## 1. 15회차가 확정한 것 (요약 — 상세는 정본 문서)

| # | 내용 |
|---|---|
| **1** | ★★ **재적합 교락.** `bayGrid.ts:508` 의 `if (!calib) return ...` 이 ③ 근변선 재적합 블록(523~556) **앞**에 있다 → `calibrateHeight:false` 는 지상고 보정과 **재적합을 동시에** 끈다. **14회차 `CALIB=0` 근거는 "보정+재적합을 껐더니"로 정정.** 통제군 A0(보정 ON + `refitFrontLine:false`)로 분리 → **재적합 단독이 v1 13면 중 5면**(A 13 → A0 8) |
| **2** | **증폭기 가설은 기각이 아니라 지지.** 동일 섭동(MAD 0.055)에 대한 감도 A 0.11933 · A0 0.09192 · C 0.09119 vs **B 0.00000033** → **약 3.6×10⁵ 배** |
| **3** | **단, "B 는 프레임 무관"은 거짓.** 구현자 1차 결론을 QA 가 반증 — `0.00000` 은 `toFixed(5)` 반올림 산물(실제 3.3×10⁻⁷)이고, 프레임 주입 실험에서 **0.95579 → 0.25387**. **"무관"이 아니라 "저감도"** |
| **4** | **P1 전제 붕괴.** v2 30장 전부 `attempts=2`·`settled:false` **0건** → v1 도 이미 정착본이었다. v1↔v2 차이는 "이동직후 vs 정착"이 아니라 **캡처 시각(13:38 vs 15:29)**. 동일 해시 **22/30**이고 **2:1 은 6/6 동일**이라 지시서 기준①(2:1 격차 0.112→≤0.02)은 **측정 자체가 불성립** |
| **5** | **성공기준 판정**: ① 미측정 / ② 무회귀 ±0.002 **B·C 모두 미달** / ③ **B·C 통과** → **②를 아무도 못 넘겨 16회차 서비스 배선 후보 없음, 서비스 무변경** |
| **6** | **C 2:2 = 0 붕괴는 버그가 아니다**(QA 확정). 득표 동점 해소 문제 — C 가 고른 5.0327m 에서 군집이 파편화돼 2:2 동점이 되고 `roiAutoFuse.ts:337` 최소인덱스 규칙이 **틀린 행에 latch 한 d0** 을 뽑는다. 2:2 는 4.950→5.033 에서 갈리는 **임계 프리셋** |
| **7** | **2:1 잔여 결손**: 증폭기 제거 후에도 B 6면이 0.9256~0.9375 에 **균일하게** 깔려 0.95 에 평균 **0.018** 부족 — **산포가 아니라 일정 편향**이다 |
| **8** | **오버레이 직접 관측**: 자가보정 지상고가 같은 PTZ 에서 `1:1` **4.96m(v1)→5.05m(v2)**, `1:2` **4.98→5.22(+4.8%)** |
| **9** | **"13면"은 프레임 운이었다** — 라이브 1:1 0.85540 이 골든 v2 0.85541 과 사실상 동일. v1 의 **0.97474 쪽이 예외** |

---

## 2. 이번 세션에서 마스터가 결정한 것

| # | 결정 | 반영 |
|---|---|---|
| 1 | `selectedCameraId` → **`simulator-1`** | `config/tools.config.json:98` 적용 완료 |
| 2 | **서버 재기동 승인** | 완료 · 13020 이 시뮬(13110)에 결선 확인 · **P0 ② 검증 통과(0.85540)** |
| 3 | **픽스처 git 미커밋** | `.gitignore:31` 에 v1·v2 등재. `git check-ignore` 확인 완료 |
| 4 | **실카 대기** | P3 는 보고만, 실행 0건 |

### ★ P0 관련 정정 (지시서 문언과 실제가 달랐다)
- 커밋본(HEAD) 값은 시뮬이 아니라 **`real-camera-1`** 이었다. 12:46 변경은 `real-camera-1`→`real-camera-2` **+ 192.168.0.154 평문 비밀번호 추가** → **마스터의 실카 정찰 작업으로 추정**. 실카 재개 시 되돌릴 항목.
- 지시서의 "승인 불가 시 P1 캡처 불가"는 **사실이 아니었다**. 골든 캡처는 Unity 13110 `/rpc` 직결이라 `selectedCameraId` 와 무관하다(`roiAutoFuse.ts:140-141`).

---

## 3. ▶ 16회차 착수 후보 (전부 **미착수**. 리더가 이번 라운드 실행 금지시킨 것들)

우선순위 순. **착수 전 마스터 결정이 필요한 항목은 §4 에 있다.**

| 순위 | 항목 | 근거 | 예상 비용 |
|---|---|---|---|
| **1** | **`bayGrid.ts:508` 조기반환 수정** — `calib===null` 이어도 ③ 재적합에 도달하게. 그래야 **"고정 지상고 + 재적합 ON"** 조합이 처음으로 가능해진다(현재 구조상 불가) | 15회차 §1-1. 이 조합이 곧 "증폭기는 끄고 재적합 이득은 살리는" 후보다 | `src/ground` 1줄 + 회귀. **서비스 영향 있음 — 설계·검증 필수** |
| **2** | **`2:1` 일정 편향 0.018 표적화** | 15회차 §1-7. 산포가 아니라 계통이라 **단일 원인**일 가능성이 높다 | 분석 라운드 1회 |
| **3** | **`roiAutoFuse.ts:337` 대표 시점 동점 규칙 재검토** | C 2:2 붕괴의 직접 원인. "최소 인덱스 우선"이 **틀린 행에 latch 한 기저 시점**을 뽑는다 | 도구 레벨 수십 줄 |
| **4** | **`roiAutoFuse.ts:72-73` 골든 판정을 `resolve()` 로 통일** | 현재 **문자열 완전일치**라 끝 슬래시 하나면 `golden=false` 로 빠져 **라이브 캐시를 v1 픽스처 디렉터리에 쓴다**. 이번엔 미발현이나 픽스처 오염 위험 | 2줄 |
| 5 | B+/C+ (도구 레벨 ③ 재현) | 순위 1이 되면 **불필요해진다** — 먼저 1을 보라 | — |

---

## 4. ★ 마스터 결정 대기 (착수 전 필요)

| # | 항목 | 리더 권고 |
|---|---|---|
| **1** | **② 무회귀 미달을 회귀로 볼 것인가, 트레이드오프로 볼 것인가** — B 는 `1:1` **−0.019** 인데 `2:1` **+0.078**. 단순 악화가 아니다 | **트레이드오프로 보고 순위 1(508 수정) 이후 재측정** 권고. 지금 판정하면 교락된 상태를 판정하는 것 |
| **2** | **`tools.config.json` 에 남은 평문 비밀번호**(`real-camera-2` / 192.168.0.154) | 마스터가 넣으신 것이라 **손대지 않았다.** 이 파일은 **git 추적 대상**이라 커밋 시 저장소에 평문으로 남는다 — 환경변수 이전 권고 |
| **3** | **실카 이행 선행물 5종** 착수 여부 | 정본 문서 P3 장 참조(각 항목에 결정 주체·예상 작업량 있음). 마스터가 "실카 대기"라 하셨으므로 **보류 중** |
| **4** | **미커밋 작업 전체의 커밋 여부** | 15회차 소스·문서가 전부 미커밋이다(§5). 픽스처만 제외 결정됨 |
| **5** | **`1:3` 정본 축 뒤바뀜 · `1:2` 정본 5~12% 과대** (14회차 이월) | 실카 우선 방향에서 **후순위 보류** 권고 |

---

## 5. 현재 파일 상태 (재개 시 확인용)

### 이번 라운드 변경 (전부 **미커밋**)
| 파일 | 성격 |
|---|---|
| `src/tools/roiAutoGoldenV2.ts` | **신규** — 정착 프레임 캡처(해시 2연속 동일, 상한 30초, `settled` 기록) |
| `src/tools/roiAutoFuse.ts` | **확장** — 프레임 출처 v1/v2 + 모드 A/A0/B/C |
| `src/tools/roiAutoOverlay.ts` | **확장** — 골든 세트(JPG) 읽기 + 디더 인덱스 인자 |
| `config/tools.config.json` | `selectedCameraId` → `simulator-1` (P0) |
| `.gitignore` | 골든 픽스처 v1·v2 제외 등재 |
| `docs/20260729_171526_...15회차_골든v2와_증폭기통제.md` | **정본 문서**(P3 실카 선행물 포함) |
| `_workspace/{01b,02o,03b,04b}_*_round15.md` | 설계·구현·검증·영향도 |
| `.claude/checkpoints/CHECKPOINT.md` | 15회차 반영 |
| `메모/memo.md` | 15회차 항목 추가 |

### 무변경 (확인됨)
`src/ground/*` · `src/rpc/services/roiAuto.ts` · `data/Place01/PtzCamRoi.json`(정본) · DB · `test/fixtures/roiAutoGolden/`(v1, 30/30 sha256 일치)

### 생성 데이터 (git 제외)
`test/fixtures/roiAutoGolden_v2/` 30장+manifest(11MB) · `reports/overlay_r15/` PNG 10장 · `_workspace/15_*.log`

---

## 6. 실행 환경 메모 (다음 세션이 헤매지 않게)

| 항목 | 사실 |
|---|---|
| Unity 시뮬 | `http://localhost:13110/rpc` — **가동 중**. 골든 캡처는 여기 **직결**이라 `selectedCameraId` 와 무관 |
| SettingAgent 13020 | `npm start`(nodemon). **`simulator-1` 결선 확인** |
| SettingAgent 13021 | **자체 config 사본**(`SettingAgent-13021/config/`)을 쓴다 — 메인 config 수정이 여기엔 **반영되지 않는다**. 이미 `simulator-1` |
| **config 반영** | `nodemon.json` 이 `src` 만 감시 → **`config/` 변경은 재기동해야 먹는다** |
| 프레임 취득 전 | **반드시 `roi.show2d {visible:false}`** — 시뮬이 초록 박스를 렌더해 도색선을 덮는다(F2) |
| 벤치 런타임 | `roiAutoFuse` 1회 약 **26분**(5프리셋 × 6시점 × 변형). 모드 4개면 그 배수 |

---

## 7. 밟지 말 것 (이번 라운드에서 새로 확인된 함정)

1. **`toFixed(5)` 로 "동일"을 판정하지 마라.** 이번에 라운드 최대 결론이 이것 때문에 뒤집힐 뻔했다. 동일성 주장은 **원시 배정도로 확인**하라.
2. **`calibrateHeight:false` 는 단독 실험이 아니다.** 재적합도 같이 꺼진다(`bayGrid.ts:508`). 지상고 정책만 보려면 **A0**(보정 ON + `refitFrontLine:false`)를 써라.
3. **프레임 해시 없는 IoU 는 무효**(F13). 보고에 반드시 병기.
4. **미측정을 보간으로 채우지 마라.** 14회차에 이 관행이 `2:1` 분포를 정반대로 추정하게 만들었다.
5. **반증 목록 20건**(`_workspace/00_leader_handoff.md` · 14회차 문서 §16)을 재시도하지 마라.
6. **정본·DB 쓰기 금지** · `roi.auto.apply` 금지 · DB 쓰기는 `slot.roi.sync` 경로만(F14).
</content>
